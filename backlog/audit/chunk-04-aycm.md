# Audit — Chunk 04: AYCM tracker
Audit commit: ff23984
Specek: documentation/Features/AYCM tracker.md, documentation/Subfeatures/AYCM elfogadóhely hozzáadása.md, documentation/Subfeatures/AYCM Check-In.md, documentation/Subfeatures/AYCM Statisztikák.md
Kód: backend/src/main/java/hu/bumler/lm2/aycm/ (Partner/PriceRule/CheckIn/Settings × Controller/Service/Mapper/Entity/Repository/SyncDataLoader), Flyway V26/V27/V28, backend/.../common/DeterministicUuid.java, openapi/paths/aycm-*.yaml + components/schemas/Aycm*.yaml; frontend/src/app/pages/menu/aycm/ (aycm-dashboard/check-in/partner-list/partner-edit/stats page-ek + aycm-price-rule.ts / aycm-pass-cost.ts / aycm-stats.ts pure modulok), frontend/src/app/core/data/aycm-{partner,check-in,settings}.repository.ts, core/storage/{sqlite,http}-storage-backend.ts + local-database.service.ts, core/sync/uuid.ts, app.routes.ts, core/config/feature-flags.service.ts, assets/config/features.json
Tesztek: backend AycmCheckIn{Integration,Service}Test, AycmPartner{Integration,Service}Test, AycmPriceRuleServiceTest, AycmSettings{Integration,Service}Test (7 fájl); frontend aycm-{check-in,dashboard,partner-edit,partner-list,stats}.page.spec.ts + aycm-{pass-cost,price-rule,stats}.spec.ts (a repository-knak nincs saját spec-je — a repo + page kód közvetlenül olvasva)

## documentation/Features/AYCM tracker.md

| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték (fájl:szimbólum / teszt) | Teendő |
|---|---|---|---|---|
| 1 | Belépés a Menüből; egyetlen `menu.aycm` flag → Menü-pont + hub + három gyerek; flag ki → menü rejtve (`Jelenlegi működés`, `Feature flag`, `UI/UX`) | Implemented | `features.json:19` `"menu.aycm": true`; `feature-flags.service.ts:25,63`; `menu.page.ts:42` `aycmEnabled`; `app.routes.ts:137-175` `path:'aycm'` + `canActivate:[featureFlagGuard('menu.aycm')]` a fa tetején (minden gyerek alatta) | — |
| 2 | Vékony dashboard + drill-down a három gyerekre; bérlet-kötés a hubon (`AycmSettings`) (`Jelenlegi működés`) | Implemented | `aycm-dashboard.page.ts` `AycmDashboardPage` (tiszta fogyasztó, nincs saját entitás); `app.routes.ts` gyerek route-ok `partners` / `check-in` / `stats`; bérlet-kötés `openPass()` action-sheet | — |
| 3 | Ownership user-owned; nincs hivatalos AYCM-API / térkép / import (`Jelenlegi működés`) | Implemented | `aycm_settings.user_id NOT NULL REFERENCES users` (V28); nincs import-/térkép-kód sehol az `aycm/`-ben | — |
| 4 | Nem scope (MVP): hivatalos partner-import; térkép; éjfélen átnyúló ársáv; több belinkelt kiadás; több Check-In / nap; naptár-producer; értesítés; 4. gyerek (`Jelenlegi működés`) | Describes-future | Kényszerítve: `aycm_settings` egyetlen `linked_recurring_expense_id` oszlop; `aycm_price_rule_time_order CHECK (start_time < end_time)` + `end` max `24:00`; `idx_aycm_check_in_user_id_check_in_date … WHERE deleted = false` partial unique; nincs 4. route | — (lefedi: #10, #12, #13, #14) |
| 5 | `AycmSettings` (1:1 user): `id` UUID kliens generálja első mentéskor; `linkedRecurringExpenseId` opcionális UUID → `RecurringExpense`, **nincs DB-FK** a Pénzügyek táblára; `createdAt`/`updatedAt` audit; nincs saját `amountHuf`; üres start = nincs link (`Entitás`) | Implemented | `V28__aycm_settings.sql` (`linked_recurring_expense_id uuid` nullable, nincs FK; audit + trigger; `idx_aycm_settings_user_id` unique); `AycmSettings.yaml` (csak `id` + `linkedRecurringExpenseId`); `aycm-settings.repository.ts` `linkExpense()` | — |
| 6 | Picker: csak **beszámított** kiadás (`deleted = false ∧ active = true`); link törlése (null) megengedett (`Entitás`) | Implemented | `aycm-dashboard.page.ts:485` `items().filter(e => !e.deleted && e.active)`; `openPass()` `PICKER_CLEAR` → `linkExpense(null)` csak ha `currentId !== null`; `aycm-pass-cost.ts` `countsInMonthlyEquivalent` import | — |
| 7 | Deep-link: Pénzügyek flag be → új `RecurringExpense` create, visszatéréskor a létrehozott `id` belinkelhető; flag ki → nincs create, a picker üres lehet (`Entitás`) | Implemented | `openPass()` `PICKER_NEW_EXPENSE` gomb → `navigate([NEW_EXPENSE_URL], { queryParams: { returnTo: HUB_URL } })`; `consumeCreatedExpenseParam()` `if (createdId === null || !this.financeEnabled) return;`; flag ki → külön statikus kártya `PASS_NO_FINANCE` magyarázattal, picker nem nyílik | — |
| 8 | Visszatérés mechanizmusa: `returnTo=/tabs/menu/aycm` query param → a create form mentés után visszanavigál, a friss `id`-t `createdExpenseId` paraméterben; a hub auto-linkeli és `replaceUrl`-lel eltávolítja; megszakítás → nincs param, normál nyílás (`Entitás`) | Implemented | `recurring-expense-edit.page.ts:77,97,143-144` (`returnTo` olvasás csak `new`-nél; `navigate([returnTo], { queryParams: { createdExpenseId: saved.id } })`); `aycm-dashboard.page.ts:463-473` `consumeCreatedExpenseParam()` → `navigate([], { queryParams: {}, replaceUrl: true })` + `settingsRepo.linkExpense(createdId)`; `aycm-dashboard.page.spec.ts:170,178` | — |
| 9 | Havi bérlet a belinkelt sor `monthlyEquivalentHuf` — SSOT Rendszeres kiadások; a hub **nem** másolja a képletet (`Bérletköltség`) | Implemented | `aycm-pass-cost.ts` `import { monthlyEquivalentHuf } from '../finance/recurring-expense-math'`; `passCostHuf = monthlyEquivalentHuf(row) * monthCount`; `aycm-pass-cost.spec.ts:56` „using the SSOT formula" | — |
| 10 | `passCostComputable` = Pénzügyek flag be ∧ van `linkedRecurringExpenseId` ∧ a sor beszámított; különben megéri-e kártya `~`/homokóra; nincs saját összeg-fallback (`Bérletköltség`) | Implemented | `aycm-pass-cost.ts` `passCostComputable(financeEnabled, settings, expenses) = financeEnabled && linkedCountingExpense(...) !== null`; `aycm-dashboard.page.ts` `worthItHuf` computed → `null` ha `!passComputable()`; `aycm-dashboard.page.html` `@else { ~ }`; `aycm-pass-cost.spec.ts:47` | — |
| 11 | Látogatás értéke SSOT a Check-In snapshot `visitValueHuf`; `visitValueHuf = listPriceHuf`; `coPaymentHuf` metaadat, nem adódik hozzá (`Látogatás értéke`) | Implemented | `aycm-check-in.page.ts:755` `visitValueHuf: rule?.listPriceHuf ?? 0` (külön `coPaymentHuf` mező, nem összegzve); `V27` komment + `AycmCheckIn.yaml` „visitValueHuf equals listPriceHuf; coPaymentHuf is metadata only"; `aycm-check-in.page.spec.ts:127` „copay not added" | — |
| 12 | Hub e havi Σ: aktuális naptári hónap (kliens TZ) élő Check-Injeinek `visitValueHuf` összege; üres → **0 Ft** (nem `~`); látogatásszám ugyanennek a darabszáma (0 OK) (`Látogatás értéke`, `UI/UX`) | Implemented | `aycm-dashboard.page.ts` `monthRange = windowRange('THIS_MONTH', today())`, `summary = summarize(filterCheckIns(...))`; `aycm-stats.ts` `filterCheckIns` `!c.deleted && c.checkInDate >= from && <= to`, `summarize` count + reduce; `aycm-dashboard.page.html` mindig `{{ visitValueSumHuf() | number }}` (nincs `~` ág); `aycm-dashboard.page.spec.ts:125` | — |
| 13 | Megéri-e (hub): ha `passCostComputable` → `Σ visitValueHuf − monthlyEquivalentHuf` (előjeles egész Ft, nincs 0-ra clamp); különben `~`; a Σ ettől függetlenül szám (`Látogatás értéke`) | Implemented | `aycm-pass-cost.ts` `worthItHuf(a, b) = a - b` (nincs clamp); `aycm-dashboard.page.ts` `worthItHuf` computed; `.negative` class ha `< 0`; `aycm-pass-cost.spec.ts:70` „signed difference with no clamp"; `aycm-dashboard.page.spec.ts:131` | — |
| 14 | Check-In hub-szabályok: max 1 / user / naptári nap (kliens TZ) élő sorokra; múlt és jövő szabad; második create ugyanarra a napra → validációs hiba; a meglévő szerkeszthető (`Check-In hub-szabályok`) | Implemented | `idx_aycm_check_in_user_id_check_in_date … WHERE deleted = false` (V27); `AycmCheckInService.applyFields` `findByUserIdAndCheckInDateAndDeletedFalse … throw new UniqueViolationException(... conflict.getId())`; `aycm-check-in.page.ts` `checkInForDate()` → edit mód; nincs dátum min/max a `<ion-input type="date">`-en; `AycmCheckInServiceTest:68`, `AycmCheckInIntegrationTest:64` | — |
| 15 | Rögzítéskor/szerkesztéskor snapshot (partnernév, sáv címke, `listPriceHuf`, `coPaymentHuf`, `visitValueHuf`); szerkesztés = újraillesztés a jelenlegi sávokkal; nincs illeszkedő sáv → `visitValueHuf = 0`, sárga, a sor mégis mentődik (`Check-In hub-szabályok`) | Implemented | `aycm-check-in.page.ts` `matchedRule` computed → `matchPriceRule(rulesFor(id), date(), time())`; `save()` snapshot összeállítás; `matchedRule()` null → `<ion-text color="warning">` + `?? 0` fallbackek, `save()` fut tovább; `aycm-check-in.page.spec.ts:139` „zero snapshot … in a gap" | — |
| 16 | Árszabály hub-szabályok: idősáv + ár, `[startTime, endTime)` félig zárt; nincs külön nyitvatartás; azonos héten napon nincs átfedés; nincs éjfél-átlépés; nap vége `endTime = 24:00` (`Árszabály hub-szabályok`) | Implemented | `aycm-price-rule.ts` `minutesOfDay` (`24:00`→1440, `24:xx`→NaN), `rulesOverlap`, `matchPriceRule` `start <= min && min < end`; `AycmPriceRuleService.parseMinutes(..., allowEndOfDay)` + overlap-loop; `V26` `CHECK (start_time < end_time)`; `AycmPriceRuleServiceTest:100,111,121` | — |
| 17 | A `linkedRecurringExpenseId` **itt** él, nem a Pénzügyekben; a Pénzügyek flag független (`Gyerekek`, `Feature flag`) | Implemented | `aycm_settings.linked_recurring_expense_id` (V28); `recurring_expense` táblán nincs AYCM-mező (Chunk-Pénzügyek); `financeEnabled = featureFlags.isEnabled('menu.penzugyek')` külön kulcs | — |
| 18 | Dashboard UI: 4 kártya — E havi látogatások (szám); E havi érték (Σ, 0 OK); Megéri-e (előjeles Ft vagy `~`, tap → Statisztikák); Bérlet (belinkelt kiadás neve + havi ekvivalens vagy CTA/deep-link, `~` ha nem computable) (`UI/UX`) | Implemented | `aycm-dashboard.page.html` — 4 `ion-card`; a Megéri-e kártya `routerLink="stats"`; a Bérlet kártya `(click)="openPass()"` + `linkedExpenseName()` / `linkedExpenseMonthlyHuf()` alcím / `PER_MONTH` | — |
| 19 | FAB / elsődleges CTA: Check-In; ha ma már van Check-In → a mai szerkesztő (ne második create); „Most" a Check-In űrlapon ma + jelenlegi idő (`UI/UX`) | Implemented | `aycm-dashboard.page.ts:476` `openCheckIn()` `hasToday ? { queryParams: { date: today() } } : {}`; `aycm-check-in.page.ts` `now()` (ma → csak idő; más nap → `loadForDate(today()).then(set time)`); `aycm-dashboard.page.spec.ts:152,160` | — |
| 20 | További belépők: elfogadóhelyek listája; statisztika; kontraszt `~`/szám (`UI/UX`) | Implemented | `aycm-dashboard.page.html` `ion-list` → `routerLink="partners"` + `routerLink="stats"`; `.negative` szín-osztály + `~` szöveg | — |
| 21 | Frontend: `AycmDashboardPage`, route `/tabs/menu/aycm`; gyerekek `/partners`, `/check-in`, `/stats` (`Architektúra/Frontend`) | Implemented | `app.routes.ts:141-174` pontosan ezek | — |
| 22 | Backend: `aycm_settings` (`id` UUID, `user_id` unique, `linked_recurring_expense_id` nullable, audit); nincs FK a `recurring_expense`-re (`Architektúra/Backend`) | Implemented | `V28__aycm_settings.sql` szó szerint | — |
| 23 | OpenAPI singleton: `GET` `PUT /api/aycm-settings`; `GET` üresen `{ linkedRecurringExpenseId: null }` (létrehozás első `PUT`-kor vagy lazy) (`Architektúra/Backend`) | Implemented | `openapi.yaml:154`, `aycm-settings.yaml` (`getAycmSettings` „Never 404s — … lazy"); `AycmSettingsService.get` → `lazy(userId)` `dto.linkedRecurringExpenseId(null)`, `id = DeterministicUuid.v5("AycmSettings:"+userId)`; `AycmSettingsServiceTest:38`, `AycmSettingsIntegrationTest:47`. Megj.: az impl a lazy válaszban `id`-t is ad (a spec csak a `linkedRecurringExpenseId: null`-t nevesíti — szuperhalmaz, összhangban a determinisztikus-id kikötéssel) | — |
| 24 | Backend-offline: dashboard olvasás helyi store-ból; `AycmSettings` `PUT` → outbox + kliens UUID; megéri-e / havi Σ pure TS (nincs homokóra a számítás miatt — `~` csak `passCostComputable = false`); Check-In / partner mutáció a gyerekek outboxa (`Architektúra/Backend-offline`) | Implemented | `sqlite-storage-backend.ts:1103-1120` `getAycmSettings` / `upsertAycmSettings` (`executeTransaction([aycmSettingsLocalWriteTask, ...enqueue.outboxTasks])`, `entityType: 'AycmSettings'`); `aycm-settings.repository.ts` `id = settings()?.id ?? await uuidV5(\`AycmSettings:${userId}\`)`; `aycm-pass-cost.ts` / `aycm-stats.ts` tiszta TS; `local-database.service.ts:1038` `aycm_settings` SQLite tábla | — |

## documentation/Subfeatures/AYCM elfogadóhely hozzáadása.md

| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték (fájl:szimbólum / teszt) | Teendő |
|---|---|---|---|---|
| 1 | User-owned partnerek + ársávjaik; teljes CRUD; nincs külön nyitvatartás-tábla (az árszabály az idősáv); Check-In csak élő partnert választ, nincs inline create (`Jelenlegi működés`) | Implemented | `AycmPartnerController` / `AycmPriceRuleController` (list/get/create/update/delete); `aycm-check-in.page.ts` `livePartners = partners().filter(p => !p.deleted)`, üres → `GO_TO_PARTNERS` CTA (nincs inline űrlap) | — |
| 2 | Nem scope (MVP): hivatalos import; térkép/cím; partner `active`; éjfél-átnyúló sáv; szabály-duplikálás; seed; undelete; inline partner-create a Check-Inről; külön top-level szabálylista (`Jelenlegi működés`) | Describes-future | `aycm_partner`-ben nincs `active`/cím/GPS oszlop (V26); nincs „duplikál" gomb a `aycm-partner-edit.page`-en; nincs `aycm/rules` route; nincs seed migráció | — (lefedi: #10, #14) |
| 3 | `AycmPartner`: `id` UUID kliens; `name` kötelező, trim után nem üres, egyedi a user élő partnerei közt (Névegyediség), törölt név újra felvehető; `notes` opcionális; `deleted` soft (default false, listák/picker szűrik); audit (`Entitás — AycmPartner`) | Implemented | `AycmPartnerService.applyFields` (`name.trim()`, `ValidationException` üresre, `NameNormalizer.normalize` + `findByUserIdAndNameNormalizedAndDeletedFalse` → `UniqueViolationException(... conflict.getId())`); `idx_aycm_partner_user_id_name_normalized … WHERE deleted = false` (V26); `AycmPartnerServiceTest:66,83`, `AycmPartnerIntegrationTest:68` | — |
| 4 | Nincs `active`/cím/GPS; üres start (nincs seed); 0 szabály menthető (`Entitás — AycmPartner`) | Implemented | V26 séma; `aycm-partner-edit.page.ts` `savePartner()` szabály nélkül is ment, utána a szerkesztőn maradunk sávot adni | — |
| 5 | `AycmPriceRule`: `id` UUID kliens; `partnerId` kötelező; `label` opcionális trim → fallback idősáv-név; `appliesMon…Sun` 7 boolean, legalább egy `true`; `startTime` HH:mm 00:00–23:59; `endTime` startTime után, max `24:00`, félzárt `[start,end)`, nincs éjfél-átlépés; `listPriceHuf` egész ≥0 (= `visitValueHuf`); `coPaymentHuf` egész ≥0 default 0 (metaadat); `deleted` soft; audit (`Entitás — AycmPriceRule`) | Implemented | `V26` (`applies_* boolean`, `aycm_price_rule_has_day CHECK`, `start_time < end_time CHECK`, `list_price_huf/co_payment_huf >= 0 CHECK`, `co_payment_huf DEFAULT 0`); `AycmPriceRuleService.parseMinutes` (`allowEndOfDay` → 1440 max, egyébként 1439); `AycmPriceRule.yaml` `endTime pattern … |24:00`; `aycm-price-rule.ts` `displayLabel` fallback | — |
| 6 | Átfedés: azonos élő partneren, adott héten napon (ahol mindkét sáv `applies*`), a `[start,end)` diszjunkt; szomszédos OK; rés OK; ütközés → validációs hiba a két sáv nevével; kliens + szerver (`Átfedés`) | Implemented | `aycm-price-rule.ts` `rulesOverlap` (`sharesDay` + `aStart < bEnd && bStart < aEnd`); `aycm-partner-edit.page.ts:1155` `this.rules().find(... rulesOverlap(candidate, r))` → `RULE_ERROR_OVERLAP { label: displayLabel(clash) }`; `AycmPriceRuleService.applyFields` szerveroldali overlap-loop → `ValidationException`; `AycmPriceRuleServiceTest:111,120,130`, `AycmPartnerIntegrationTest:124` | — |
| 7 | `displayLabel(rule)` = trim-elt nem üres label, különben `startTime–endTime`; a Check-In snapshot `ruleLabel` = `displayLabel` a rögzítéskor; későbbi címke-szerkesztés a múltat nem írja felül (`Megjelenő sávnév`) | Implemented | `aycm-price-rule.ts` `displayLabel`; `aycm-check-in.page.ts` `save()` `ruleLabel: rule ? displayLabel(rule) : ''` a local row-ra menti; a szerver a snapshotot változatlanul tárolja (`AycmCheckInService` nem hívja a matchet); `aycm-price-rule.spec.ts:44,48` | — |
| 8 | `matchPriceRule` SSOT (a Check-In hívja, nem másolja): élő szabályok a partneren → a dátum hét napján `applies*` → `startMin ≤ checkInMin < endMin` → átfedés-mentesség miatt legfeljebb egy találat; kimenet: szabály vagy nincs találat (`Illesztés SSOT`) | Implemented | `aycm-price-rule.ts` `matchPriceRule(rules, checkInDate, checkInTime)` (`weekdayIndex`, `rule.deleted` szűrés, `start <= minutes && minutes < end`, `.find() ?? null`); `aycm-check-in.page.ts:543` `import { displayLabel, matchPriceRule } from './aycm-price-rule'`; `aycm-price-rule.spec.ts:77-93` | — |
| 9 | Nincs találat → Check-In `visitValueHuf = 0`, `listPriceHuf = 0`, `coPaymentHuf = 0`, `ruleLabel` üres/„—", sárga; a sor mentődik (`Illesztés SSOT`) | Implemented | `aycm-check-in.page.ts` `save()` `ruleId: rule?.id ?? null`, `ruleLabel: rule ? … : ''`, `listPriceHuf: rule?.listPriceHuf ?? 0`, `coPaymentHuf: rule?.coPaymentHuf ?? 0`, `visitValueHuf: rule?.listPriceHuf ?? 0`; sárga `PREVIEW_NO_MATCH` | — |
| 10 | CRUD partner: lista, create, edit, törlés; duplikálás nincs; create: `name` auto-focus, notes opcionális, szabályok utána a szerkesztőn (`CRUD — partner`) | Partial | `aycm-partner-list.page` + `aycm-partner-edit.page` (create/edit/törlés, nincs duplikál). `name` **auto-focus nincs** kódolva (`ion-input`-en nincs `autofocus`/`setFocus`) — kozmetikai hiány | Draft #15 |
| 11 | Törlés partner: megerősítés a névvel + élő sávok száma; soft delete a partnerre **és** minden élő szabályára (cascade); Check-In sorok **nem** törlődnek; pickerből kiesik (`CRUD — partner`) | Implemented | `aycm-partner-list.page.ts` `confirmDelete` `DELETE_CONFIRM_MESSAGE { name, count: liveRuleCount(id) }`; `AycmPartnerService.delete` → `entity.softDelete()` + loop a `findByPartnerId…DeletedFalse` sávokon `rule.softDelete()`; a Check-In táblát nem érinti; `sqlite-storage-backend.ts:942-948` helyi cascade `UPDATE aycm_price_rule SET deleted = 1 … WHERE partner_id = ? AND deleted = 0`; `AycmPartnerServiceTest:121`, `AycmPartnerIntegrationTest:136` | — |
| 12 | CRUD szabály: csak a partner szerkesztőn, nincs top-level route; lista `startTime` növekvő majd `displayLabel`; create/edit/törlés (confirm a `displayLabel`-lel); soft delete; múltbeli Check-In érintetlen; mentés: nap-flag, idősáv, árak, overlap (`CRUD — szabály`) | Implemented | `aycm-partner-edit.page.ts` `rules` computed `sort(a.startTime.localeCompare(b.startTime) || displayLabel(a).localeCompare(displayLabel(b)))`; `confirmDeleteRule` `RULE_DELETE_CONFIRM_MESSAGE { label: displayLabel(rule) }`; `saveRule` validál nap/idő/ár/overlap; `AycmPriceRuleService.delete` idempotens soft delete | — |
| 13 | Soft delete szerződés: tombstone, idempotens `DELETE`, saját törölt `GET` → 200 + `deleted`, `PUT` töröltön nem undo; soha nem syncelt draft → hard remove + outbox purge (`CRUD — szabály`, `Backend-offline`) | Implemented | `AycmPriceRuleService.update` `if (entity.isDeleted()) throw new EntityDeletedException`; `get` a `findByIdAndUserId`-t adja (törölt sort is → 200 + `deleted`); `delete` idempotens; `sqlite-storage-backend.ts:937,945,1006` `isNew`/local-only ág `DELETE FROM …` vs `UPDATE … SET deleted = 1`; `AycmPriceRuleServiceTest:150`, `AycmPartnerServiceTest:110,138` | — |
| 14 | UI: belépés a hubról; route `/tabs/menu/aycm/partners`, `…/new`, `…/:id`; lista: kereső (`name` + `notes`), sor: név + élő sávszám, sorrend `name`, üres → CTA, szűrt üres ≠ globális üres (`UI/UX`) | Implemented | `app.routes.ts:146-162`; `aycm-partner-list.page.ts` `matchesSearch(query, p.name) || matchesSearch(query, p.notes ?? '')`, `sort(name.localeCompare)`, `liveRuleCount`, `isGlobalEmpty` / `isFilteredEmpty`; `aycm-partner-list.page.spec.ts:54,63,72,80` | — |
| 15 | Create/edit: név, notes; alatta sávok (hozzáadás/szerkesztés/törlés); idő platformos time picker; `endTime`-nál **Nap vége (`24:00`)** opció; árak egész Ft; 7 nap checkbox i18n; Check-In picker csak `deleted = false`, üres → CTA erre a listára (`UI/UX`) | Implemented | `aycm-partner-edit.page.ts` `ruleForm` (`endOfDay` control → `saveRule()` `endTime = v.endOfDay ? '24:00' : v.endTime`; `editRule` visszaalakítás `rule.endTime === '24:00'`); `listPriceHuf`/`coPaymentHuf` `number` control `Validators.min(0)`; 7 `applies*` control; `aycm-partner-edit.page.html` `ion-checkbox` × 7 + `TranslatePipe`; Check-In picker `livePartners()` | — |
| 16 | Backend táblák: `aycm_partner` / `aycm_price_rule` a leírt oszlopokkal; `(user_id, name_normalized)` partial unique `WHERE deleted = false`; szerver is ellenőrzi az overlapot élő szabályokon; Check: `list_price_huf ≥ 0`, `co_payment_huf ≥ 0`, ≥1 nap flag, `end > start` (`Architektúra/Backend`) | Implemented | `V26__aycm_partner_price_rule.sql` szó szerint (indexek + 3 CHECK + `aycm_price_rule_has_day`); `AycmPriceRuleService.applyFields` overlap-loop | — |
| 17 | OpenAPI: `GET` `POST /api/aycm-partners`; `GET` `PUT` `DELETE /api/aycm-partners/{id}` (`DELETE` = soft + cascade); `GET` `POST /api/aycm-partners/{id}/price-rules`; `GET` `PUT` `DELETE /api/aycm-partners/{id}/price-rules/{ruleId}`; idegen/más-user `id` → 404; saját törölt `GET` by id → 200 + `deleted`; `DELETE` idempotens; nincs Check-In törlés a partner `DELETE`-től (`Architektúra/Backend`) | Implemented | `openapi.yaml:142-149` + `openapi/paths/aycm-partners*.yaml` / `aycm-partner-price-rules*.yaml`; `AycmPartnerController` / `AycmPriceRuleController` a generált interfészt implementálja; `requirePartner` guard a rule-path-ra; `AycmPartnerIntegrationTest:94,105,153`, `AycmPriceRuleServiceTest:168,177,188,198` | — |
| 18 | Frontend: `AycmPartnerListPage`, `AycmPartnerEditPage` (sávok a szerkesztőn); pure TS: perc-konverzió, `displayLabel`, overlap, `matchPriceRule`; kereső; partner create → szabály `POST`: FIFO + `targetEntityId` függőség, ha ugyanabban a flowban jön létre a partner (`Architektúra/Frontend`) | Implemented | `aycm-price-rule.ts` (mind a 4 pure fn); `sqlite-storage-backend.ts:978` `upsertAycmPriceRule` → `dependsOn = await this.findLocalOnlyIds('aycm_partner', [rule.partnerId])` az outbox enqueue-ban | — |
| 19 | Backend-offline: olvasás/írás helyi store; partner/szabály create/update/delete → outbox + kliens UUID; `matchPriceRule` / overlap mindig kliens TS; partner `DELETE` helyi cascade `deleted` a szabályokon + outbox partner `DELETE`; soha nem syncelt draft hard remove + outbox purge; pull: `deleted = true` → kiesik, pending `PUT` eldobandó (`Backend-offline`) | Implemented | `sqlite-storage-backend.ts:902-1036` (`aycmPartnerLocalWriteTask` / `aycmPriceRuleLocalWriteTask` + `enqueue.outboxTasks` egy `executeTransaction`-ben; `entityType: 'AycmPartner'` / `'AycmPriceRule'`); `aycm-partner.repository.ts` `uuidV4()` + `requestDrainIfNative()`; `local-database.service.ts:970-1008` SQLite táblák | — |

## documentation/Subfeatures/AYCM Check-In.md

| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték (fájl:szimbólum / teszt) | Teendő |
|---|---|---|---|---|
| 1 | Napi belépés rögzítése: partner + dátum + idő → automatikus sávillesztés, snapshot az értékkel; nincs külön előzmény-lista (az a Statisztikák); a dátummező tölti az aznapi rekordot (`Jelenlegi működés`) | Implemented | `aycm-check-in.page.ts` `AycmCheckInPage` (nincs list-page; `date` signal → `loadForDate`); `app.routes.ts:166-169` egyetlen `check-in` route | — |
| 2 | Nem scope (MVP): több Check-In / naptári nap; inline partner-create; külön Check-In lista; 15 perces kerekítés; naptár-producer; értesítés; undelete (`Jelenlegi működés`) | Describes-future | `idx_aycm_check_in_user_id_check_in_date … WHERE deleted = false` partial unique; `<ion-input type="time">` percpontos (nincs kerekítés); nincs list route | — (lefedi: #12, #13) |
| 3 | `AycmCheckIn`: `id` UUID kliens; `checkInDate` YYYY-MM-DD, múlt/ma/jövő szabad; `checkInTime` HH:mm perc 0–1439 (soha 24:00); `partnerId` UUID (create/edit élő partner; törölt partner után a UUID megmarad); `partnerName` snapshot; `ruleId` snapshot UUID vagy null; `ruleLabel` snapshot; `listPriceHuf`/`coPaymentHuf`/`visitValueHuf` snapshot egész ≥0; `visitValueHuf = listPriceHuf`; `notes` opcionális; `deleted` soft; audit (`Entitás`) | Implemented | `V27__aycm_check_in.sql` (`check_in_date date`, `check_in_time text`, `partner_id uuid` **FK nélkül**, `rule_id uuid` nullable, `rule_label text DEFAULT ''`, 3 × `integer CHECK (… >= 0)`); `AycmCheckInService.applyFields` (`TIME_PATTERN = ([01][0-9]|2[0-3]):[0-5][0-9]`, negatív-Ft `ValidationException`); `AycmCheckIn.yaml` | — |
| 4 | Egyediség `(userId, checkInDate)` az élő sorokra; törlés után a nap újra szabad (új UUID) (`Entitás`, `Napi egyediség`) | Implemented | `idx_aycm_check_in_user_id_check_in_date … WHERE deleted = false` (V27); `AycmCheckInService.delete` soft; `AycmCheckInIntegrationTest:92` „delete frees the day", `AycmCheckInServiceTest:149` | — |
| 5 | Illesztés és snapshot: mentéskor (create és edit) a kliens hívja a `matchPriceRule`-t a jelenlegi élő sávokra a `checkInDate` hét napjával; van sáv → `ruleId`/`ruleLabel`/`listPriceHuf`/`coPaymentHuf`/`visitValueHuf = listPriceHuf`; nincs → `ruleId = null`, `ruleLabel` üres/„—", három Ft 0; `partnerName` = a kiválasztott élő partner neve; a szerver **nem** számol újra; partner/dátum/idő változás → újraillesztés, snapshot felülíródik; `notes` önmagában nem (`Illesztés és snapshot`) | Implemented | `aycm-check-in.page.ts` `matchedRule` computed (partner+date+time függő); `save()` `partnerName = livePartners().find(...)?.name ?? ''`; `AycmCheckInService` csak tárol (nem match-el) — `AycmCheckInServiceTest:130` „reMatchesNothingServerSide"; `notes`-only edit a `snapshotFrozen` ágon a régi snapshotot menti | — |
| 6 | Későbbi ársáv-/partnernév-szerkesztés a már mentett sort nem bántja, amíg a user nem szerkeszti újra (`Illesztés és snapshot`) | Implemented | a `aycm_check_in` snapshot-oszlopok statikusak; `AycmPartnerService`/`AycmPriceRuleService` nem nyúl a `aycm_check_in`-hez; `aycm-check-in.page.ts` csak explicit `save()`-re ír | — |
| 7 | Napi egyediség: második élő Check-In ugyanarra a `checkInDate`-re → validációs hiba, a UI a meglévőt tölti (edit); dátum átírása foglalt napra → **409** `UNIQUE_VIOLATION` (ugyanaz a hibakód, mint create-nél; a cél nap élő sora marad) (`Napi egyediség`) | Implemented | `AycmCheckInService.applyFields` `findByUserIdAndCheckInDateAndDeletedFalse … filter(id != entity.id) … throw UniqueViolationException(... conflict.getId())` (create és update közös); `aycm-check-in.page.ts:739` `targetId = editingId() ?? existing?.id` (foglalt napra edit); `AycmCheckInIntegrationTest:77` „put rewriting date onto taken day returns 409", `AycmCheckInServiceTest:68` | — |
| 8 | CRUD: create/szerkesztés/törlés; duplikálás nincs; nincs lista-képernyő; törlés: megerősítés (`checkInDate` + `partnerName`) → soft delete, kiesik a hub havi Σ/darabszámból, a nap újra szabad; soft delete szerződés; soha nem syncelt draft → hard remove + outbox purge; partner kötelező; törölt partner nem választható új mentéshez, a régi sor olvasható/törölhető/notes szerkeszthető, partner/idő csere csak élő pickerre (`CRUD`) | Implemented | `aycm-check-in.page.ts` `delete()` `DELETE_CONFIRM_MESSAGE { date, name }`; `AycmCheckInService.update` `EntityDeletedException` töröltre; `snapshotFrozen` computed (`row.partnerId === partnerId() && !selectedPartnerLive()`) → dátum/idő `[disabled]`, `save()` csak `notes`-t ír; `sqlite-storage-backend.ts:1074` local-only ág hard `DELETE` | — |
| 9 | 0 Ft / sárga: nincs illeszkedő sáv (0 szabály, lyuk, rossz nap) → sárga figyelmeztetés, snapshot 0, mentés szabad, nincs plusz confirm (`0 Ft / sárga`) | Implemented | `aycm-check-in.page.html` `@if (matchedRule()) { color="success" } @else { color="warning" PREVIEW_NO_MATCH }`; `save()` nem kér extra confirmot; `aycm-check-in.page.spec.ts:139` | — |
| 10 | UI belépés: hub FAB/CTA; ma nincs élő Check-In → create default **ma + jelenlegi idő**; ma van → a **mai** szerkesztő; route `/tabs/menu/aycm/check-in` opcionális `?date=YYYY-MM-DD`; date-váltás → edit ha van élő sor, különben create a kiválasztott dátummal; reaktív előnézet zöld/sárga; törlés confirm (csak edit) (`UI/UX`) | Implemented | `aycm-check-in.page.ts` `ngOnInit` `dateParam ?? resolveInitialDate()`, `loadForDate` (existing → edit mezők, nincs → create; 1 élő partner → auto-kiválasztás); `now()`; `@if (isEdit)` törlés gomb | — |
| 11 | UI űrlap: **partner picker (élő, [[Szöveges keresés]])**; dátum; idő percpontos; **Most**; üres partnerlista → CTA az elfogadóhely-listára (nincs inline create) (`UI/UX`, `Architektúra/Frontend`) | Partial | `aycm-check-in.page.html` a partner picker `<ion-select interface="action-sheet">` — **nincs szövegkereső mező** benne (szemben a partner-listával, ami `matchesSearch`-öt használ). A többi elem kész: `type="time"` percpontos, `now()` gomb, `hasNoPartners()` → `GO_TO_PARTNERS` | Draft #9 |
| 12 | Backend: `aycm_check_in` a leírt oszlopokkal; partial unique `(user_id, check_in_date) WHERE deleted = false`; nincs FK-kényszer élő partnerre; OpenAPI: `GET` `POST /api/aycm-check-ins` (lista `?from=&to=`; create; unique → **409**), `GET` `PUT` `DELETE /api/aycm-check-ins/{id}` (edit = `PUT` új snapshot; dátum-átírás foglalt napra → **409** `UNIQUE_VIOLATION`; `DELETE` soft); idegen `id` → 404; saját törölt `GET` → 200 + `deleted`; `DELETE` idempotens; a szerver **nem** futtatja `matchPriceRule`-t, elfogadja a snapshotot (nemnegatív Ft) (`Architektúra/Backend`) | Implemented | `V27`; `openapi.yaml:150-153` + `aycm-check-ins*.yaml`; `AycmCheckInService.list` `from!=null && to!=null` → `…CheckInDateBetween…`; `AycmCheckInController`; `AycmCheckInIntegrationTest:55,64,77,103,117`, `AycmCheckInServiceTest:86,97,163,175` | — |
| 13 | Frontend: `AycmCheckInPage`, nincs lista-page; pure TS `matchPriceRule` import + snapshot összerakás + napi egyediség a helyi store-on (előnézet); OpenAPI kliens; mutációk offline rétegen; illesztés mindig kliens TS; soft delete `deleted = true` + outbox `DELETE`; soha nem syncelt draft hard remove + outbox purge; pull `deleted = true` → kiesik az élő napból, pending `PUT` eldobandó (`Backend-offline`) | Implemented | `aycm-check-in.repository.ts` (`checkInForDate`, `save` `uuidV4()`, `remove`, `requestDrainIfNative`); `sqlite-storage-backend.ts:1039-1100` (`aycmCheckInLocalWriteTask` + outbox egy tranzakcióban, `entityType: 'AycmCheckIn'`); `local-database.service.ts:1013` `aycm_check_in` tábla | — |

## documentation/Subfeatures/AYCM Statisztikák.md

| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték (fájl:szimbólum / teszt) | Teendő |
|---|---|---|---|---|
| 1 | Read-only összesítés az élő `AycmCheckIn` snapshotokból: időablak, megéri-e, helyszín, látogatáslista; nincs saját entitás; a hub az aktuális naptári hónapot mutatja, ez a képernyő a preset-ablakokat és a bontást (`Jelenlegi működés`) | Implemented | `aycm-stats.page.ts` `AycmStatsPage` (nincs entitás/OpenAPI/outbox); `aycm-stats.ts` pure modul; `app.routes.ts:170-174` `stats` route | — |
| 2 | Nem scope (MVP): diagram; custom dátumtartomány; „összes idő"; idei naptári év preset; copay-összeg külön kártyán; saját OpenAPI / outbox (`Jelenlegi működés`) | Describes-future | `aycm-stats.page.ts` `windows: readonly StatsWindow[] = ['THIS_MONTH','PREV_MONTH','LAST_3_MONTHS']` (csak 3, fix); nincs `<canvas>`/chart-lib; `coPaymentHuf` sehol a `aycm-stats.ts`-ben | — (lefedi: #11) |
| 3 | Adathalmaz: élő (`deleted = false`) `AycmCheckIn` sorok, ahol `checkInDate` a választott zárt intervallumban (kliens TZ); jövőbeli dátum benne van; 0 Ft-os Check-In beleszámít a darabba, a Σ-hoz 0-t ad (`Adathalmaz`) | Implemented | `aycm-stats.ts` `filterCheckIns` `c => !c.deleted && c.checkInDate >= from && c.checkInDate <= to` (lexikális összevetés YYYY-MM-DD-re helyes; jövő nincs vágva); `summarize` `visitCount: checkIns.length`; `aycm-stats.spec.ts:63` „future dates included", `:70` „0 rows → 0/0" | — |
| 4 | Ablakok: `THIS_MONTH` (aktuális hónap 1.–utolsó, `monthCount` 1), `PREV_MONTH` (előző hónap teljes, 1), `LAST_3_MONTHS` (aktuális−2 hó 1. napja … aktuális hó utolsó napja, 3); nincs custom/YTD/all-time; default `THIS_MONTH` = ugyanaz a darab/Σ mint a hubon (`Ablakok`) | Implemented | `aycm-stats.ts` `windowRange(window, todayIso)` (mindhárom ág + `monthCount`; `LAST_3_MONTHS` zero-based hónapszámítás −2); `aycm-dashboard.page.ts` `windowRange('THIS_MONTH', today())` ugyanaz a utility; `aycm-stats.spec.ts:34,38,43,48` | — |
| 5 | Összegző számok: `visitCount` = halmaz mérete (0 OK); `visitValueSumHuf` = Σ `visitValueHuf` (0 OK, nem `~`); `passCostComputable` SSOT AYCM tracker; `passCostHuf` = `monthlyEquivalentHuf × monthCount` (SSOT, nem másolt `/3 /12`); megéri-e: ha computable `visitValueSumHuf − passCostHuf` (előjeles, nincs clamp), különben `~`/homokóra; darab és Σ ettől függetlenül szám; `coPaymentHuf` nem megy a megéri-be (`Összegző számok`) | Implemented | `aycm-stats.ts` `summarize`; `aycm-pass-cost.ts` `passCostHuf` / `worthItHuf` (közös modul a hubbal); `aycm-stats.page.ts` `worthItHuf` computed → `null` ha `!passComputable()`; `aycm-stats.page.spec.ts:118,124` | — |
| 6 | Helyszín bontás: csoport `partnerId`; megjelenő név: élő partner → aktuális `AycmPartner.name`; törölt + több eltérő snapshot `partnerName` → a lexikálisan első (determinisztikus tie-break); sorok: név, `visitCount`, `visitValueSumHuf`; rendezés Σ csökkenő majd név; üres ablak → üres lista, nincs CTA (`Helyszín bontás`) | Implemented | `aycm-stats.ts` `resolveNames` (`liveById` → aktuális név; különben `c.partnerName < current` → lexikálisan első); `groupByPartner` `sort(b.sum - a.sum || a.displayName.localeCompare(b.displayName))`; `aycm-stats.spec.ts:80,94,100` | — |
| 7 | Látogatáslista: a tartomány Check-Injei `checkInDate` csökkenő majd `checkInTime` csökkenő; sor: dátum, megjelenő partnernév (ugyanaz a szabály), `visitValueHuf`; tap → AYCM Check-In `?date=YYYY-MM-DD` (`Látogatáslista`) | Implemented | `aycm-stats.ts` `visitList` `sort(b.checkInDate.localeCompare(a.checkInDate) || b.checkInTime.localeCompare(a.checkInTime))` + `resolveNames`; `aycm-stats.page.ts` `openVisit(date)` → `navigate([CHECK_IN_URL], { queryParams: { date } })`; `aycm-stats.spec.ts:111`, `aycm-stats.page.spec.ts:138` | — |
| 8 | UI: belépés a hub Megéri-e kártyáról / statisztika-belépő; route `/tabs/menu/aycm/stats`; preset választó (3); kártyák darab/Σ/megéri-e; helyszín tábla; látogatáslista; read-only; üres ablak: 0 / 0 Ft / megéri = `0 − passCost` vagy `~`, listák üresek, nincs create CTA (`UI/UX`) | Implemented | `aycm-dashboard.page.html` Megéri-e kártya `routerLink="stats"` + `ion-list` stats belépő; `aycm-stats.page.ts` `window` signal + `ion-segment` (`setWindow`); `aycm-stats.page.html` kártyák + `ion-list` bontás/látogatáslista; nincs mutáció | — |
| 9 | _Nincs backend érintettség_ (Check-In lista API: AYCM Check-In; settings: AYCM tracker) (`Architektúra/Backend`) | Implemented (verified) | nincs `aycm-stats` fájl a `backend/`-ben; `AycmStatsPage` csak `AycmCheckInRepository` / `AycmPartnerRepository` / `AycmSettingsRepository` / `RecurringExpenseRepository`-t injektál | — |
| 10 | Backend-offline: olvasás a Check-In / settings / kiadás helyi store-jából; **nincs** outbox; számítás kliens TS; `~` csak `passCostComputable = false` (`Backend-offline`) | Implemented | `aycm-stats.page.ts` `ngOnInit` csak `*.load()` hívások; `aycm-stats.ts` / `aycm-pass-cost.ts` tiszta TS; nincs `entityType` a stats-hoz | — |

## Rollup

- Állítások összesen: 57 — Implemented 51 / Partial 2 (Feladóhely #10 name-autofocus, Check-In #11 partner-picker szövegkeresés) / Missing 0 / Describes-future 4 (AYCM tracker #4, Feladóhely #2, Check-In #2, Statisztikák #2 — mind explicit „Nem scope (MVP)") / Accepted-limitation 0
- Blokkoló eltérések: nincs. A négy spec `Státusz: Kész`, és a kód lényegében fedi őket: teljes backend (4 entitás flat CRUD + snapshot + overlap + partial unique + lazy singleton), Flyway V26–V28, hand-written OpenAPI, teljes offline-sync bekötés, pure TS SSOT modulok (`aycm-price-rule.ts`, `aycm-pass-cost.ts`, `aycm-stats.ts`), 7 backend + 8 frontend teszt-fájl. Az egyetlen tartalmi (nem kozmetikai) hiány: a Check-In partner-picker nem `Szöveges keresés`-alapú.
- Draft jegyek:
  - **#9** `feat` — „AYCM Check-In: szövegkereső a partner-pickerben" → `documentation/Subfeatures/AYCM Check-In.md` (`UI/UX` + `Architektúra/Frontend`). A spec kétszer is `[[Szöveges keresés]]`-t ír elő a Check-In partner-pickerhez; az implementáció sima `ion-select interface="action-sheet"`, kereső nélkül. Sok élő partnernél nincs szűrés. (A partner-lista viszont már `matchesSearch`-öt használ — kész minta.)
  - **#15** `chore` — „AYCM partner create: `name` mező auto-focus" → `documentation/Subfeatures/AYCM elfogadóhely hozzáadása.md` (`CRUD — partner`). A spec „`name` auto-focus"-t ír; a `aycm-partner-edit.page` `ion-input`-en nincs `autofocus`/`setFocus`. Kozmetikai.
  - **#10** `feat` (Describes-future) — „AYCM: hivatalos partner-katalógus import + térkép/cím" → AYCM tracker.md / AYCM elfogadóhely hozzáadása.md. Explicit „Nem scope (MVP)"; jelenleg nincs kód. Később nyitható.
  - **#11** `feat` (Describes-future) — „AYCM Statisztikák: custom dátumtartomány + diagram + YTD/all-time + copay-kártya" → AYCM Statisztikák.md. Explicit „Nem scope (MVP)".
  - **#12** `feat` (Describes-future) — „AYCM Check-In: több Check-In / naptári nap + külön Check-In lista" → AYCM Check-In.md. Explicit „Nem scope (MVP)"; a partial unique index ma tiltja.
  - **#13** `feat` (Describes-future) — „AYCM: naptár-producer + értesítés integráció" → AYCM tracker.md / AYCM Check-In.md. Explicit „Nem scope (MVP)".
  - **#14** `feat` (Describes-future) — „AYCM ársáv: éjfélen átnyúló idősáv" → AYCM elfogadóhely hozzáadása.md. Explicit „Nem scope (MVP)"; a `start_time < end_time` CHECK + `matchPriceRule` ma kizárja.
- Spec-átírás vázlat (jelen idejűre húzás, `verifikalva` stub kitöltése):
  - **AYCM tracker.md** — a `### Jelenlegi működés` és `### Funkcionális leírás` már jelen idejű és pontos; csak a `verifikalva`/`verifikalt_commit` frontmatter kitöltendő (`ff23984`). A „megéri-e (hub)" képlet, a `passCostComputable` feltétel, a deep-link `returnTo`/`createdExpenseId` round-trip és a 4-kártyás dashboard 1:1 megvan a kódban. Nincs érdemi átírás.
  - **AYCM elfogadóhely hozzáadása.md** — jelen idejű; a `matchPriceRule` / `displayLabel` / overlap SSOT-leírás pontos. Opcionális kiegészítés: a `name` auto-focus nem valósult meg (vagy #15-tel pótolni, vagy a spec-sorból törölni). `verifikalva` kitöltendő.
  - **AYCM Check-In.md** — jelen idejű; egy pontosítás kell: a partner-picker jelenleg **nem** `Szöveges keresés`-alapú (vagy #9-cel pótolni, vagy a `[[Szöveges keresés]]` hivatkozást „egyszerű action-sheet picker"-re cserélni a `UI/UX` és `Architektúra/Frontend` szakaszban). Minden más (napi egyediség, 409 dátum-átíráson, snapshot-fagyás törölt partnernél, 0 Ft/sárga) egyezik. `verifikalva` kitöltendő.
  - **AYCM Statisztikák.md** — jelen idejű és pontos (3 preset, `windowRange`/`filterCheckIns`/`groupByPartner`/`visitList` viselkedés, lexikális tie-break, „nincs backend érintettség"). Nincs átírás; `verifikalva` kitöltendő.
- Verdikt: **GREEN** (a négy spec az implementált viselkedést írja le; egyetlen tartalmi rés — Check-In partner-picker szövegkeresés, #9 — nem blokkoló, plus egy kozmetikai #15; a Describes-future tételek mind explicit „Nem scope (MVP)").
