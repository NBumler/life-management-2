# Audit — Chunk 05: Google Calendar export
Audit commit: ff23984
Spec: documentation/Subfeatures/Google Calendar szinkronizálása.md
Kód: feature flag only

## documentation/Subfeatures/Google Calendar szinkronizálása.md

| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték | Teendő |
|---|---|---|---|---|
| 1 | Egyirányú export LM2 → Google Calendar; az LM2 sor a forrás, a Google csak tükör; nincs import, nincs kétirányú merge (`Jelenlegi működés` / `Irány és forrás-tulajdonos`) | Describes-future | Nincs export-kód sehol a `frontend/`-ben; a spec maga is `**Nem MVP:**` | — (lefedi: #1) |
| 2 | Nem MVP; az első kiadásban a feature flag `false` (`Jelenlegi működés`) | Implemented | `frontend/src/assets/config/features.json:14` → `"feladatok.googleExport": false` | — |
| 3 | Az app saját, másodlagos Google naptárt hoz létre („Life Management"), és kizárólag abba ír (`Dedikált naptár`) | Describes-future | Nincs Google API réteg, nincs naptár-létrehozó kód | — (lefedi: #1) |
| 4 | Exportált kör: élő `CalendarEvent` sorok (`deleted = false`); naplók / esedékességek nem; egy sorozat = egy Google esemény RRULE-lal, nem előfordulásonként (`Mit exportálunk`) | Describes-future | Nincs exportáló szolgáltatás; a `CalendarEvent` entitás változatlanul csak a saját backendhez szinkronizál | — (lefedi: #1) |
| 5 | Mezőleképezés (`title`→`summary`, `allDay` → exkluzív `end.date`, `frequency`+`interval` → `RRULE`, `reminders` üres); a ritmus→`RRULE` és az `allDay`→exkluzív végdátum átfordítás pure TS, unit-tesztelhető (`Mezőleképezés` / `Frontend`) | Describes-future | Nincs `*.ts` az RRULE/allDay átfordításhoz; `grep -ri` a `frontend/`-ben csak a flag-találatokat adja | — (lefedi: #1) |
| 6 | Emlékeztető kikapcsolva (`useDefault = false`, üres lista); időzóna = az exportáló eszköz aktuális IANA zónája, és a zóna része a hash-nek (`Mezőleképezés`) | Describes-future | Nincs export / hash kód | — (lefedi: #1) |
| 7 | Determinisztikus Google esemény-ID az LM2 UUID 16 bájtjának base32hex kódolásából, nem tárolt → idempotens feltöltés (`Determinisztikus Google esemény-ID`) | Describes-future | Nincs ID-leképező függvény a kódban | — (lefedi: #1) |
| 8 | Egyeztetés külön reconciler kör, nem az entitás-outboxon; kiváltók: app indulás / előtérbe jövés / esemény-mutáció (összevonva) / kézi „Exportálás most" (`Egyeztetés`) | Describes-future | `core/sync/` csak a saját backend drain/pull-ját tartalmazza; nincs export-reconciler | — (lefedi: #1) |
| 9 | Hash-alapú összevetés a device-local export-állapottal; soft-delete → Google pár törlése + állapotsor eltűnik; orphan-söprés; az egyeztetés idempotens és megszakítható (`Egyeztetés`) | Describes-future | Nincs export-állapot tábla, nincs reconciler | — (lefedi: #1) |
| 10 | Összekapcsolás: kliensoldali Google OAuth (authorization code + PKCE, client secret nincs); kizárólag online művelet, `FULL_OFFLINE`-ban az OAuth gomb `disabled` (`Összekapcsolás` / `Frontend`) | Describes-future | Nincs OAuth/PKCE kód; a `pkce-challenge` csak a `@modelcontextprotocol/sdk` dev-only tranzitív függősége (`package-lock.json:5833`), nincs a `package.json`-ban, nincs importálva | — (lefedi: #1) |
| 11 | Refresh token platform secure storage-ban, külön kulcstérben, app-frissítés után is; sosem az outboxba / a saját szerverre; minimális `calendar.app.created` jellegű scope, a konkrét sztring implementációkor véglegesítendő (`Frontend`) | Describes-future | Nincs token-tároló kód az exporthoz | — (lefedi: #1) |
| 12 | Leválasztás: token visszavonás + device-local állapot törlés, megerősítéssel opcionális Google naptár-törlés; natív-only, web buildben a feature nem jelenik meg (`Összekapcsolás`) | Describes-future | Nincs leválasztó flow; nincs `offlineCapable`/platform elágazás az exporthoz | — (lefedi: #1) |
| 13 | Hibák nem a Szinkronizációs központba mennek; saját státuszsor a beállító képernyőn (utolsó egyeztetés, hátralévő tételek, hibaüzenet, „Exportálás most"); 8 s timeout; az export soha nem blokkolja az esemény mentését; visszavont hozzáférésnél „újra összekapcsolás szükséges" állapot (`Hibák és korlátok`) | Describes-future | Nincs beállító képernyő, nincs státuszsor komponens | — (lefedi: #1) |
| 14 | `GoogleCalendarExportService` (root): összekapcsolás-állapot signal, egyeztető kör, hash-számítás, determinisztikus ID leképezés; device-local tábla (`eventId`, `hash`, `syncedAt`) + naptár-azonosító és fiók a preferenciákban; külön szűk HTTP réteg (nem a generált OpenAPI kliens) (`Architektúra/Frontend`) | Describes-future | `grep -rn "GoogleCalendarExport"` a `frontend/src`-ben: 0 találat; nincs ilyen `SCHEMA_Vn` tábla a `core/storage/local-database.service.ts`-ben | — (lefedi: #1) |
| 15 | UI belépő: Események lista fejléc → Google export (`/tabs/tasks/events/google`); a képernyő tartalma: összekapcsolás / leválasztás, cél naptár neve, egyirányúság kiírása, státuszsor, „Exportálás most", „nincs Google emlékeztető" jelzés (`UI/UX elvárások`) | Describes-future | `app.routes.ts:222–224` — az `events` alatt csak `''`, `new`, `:id` route; nincs `google`. `pages/tasks/events/`-ben nincs export / google hivatkozás (csak `export class`) | — (lefedi: #1) |
| 16 | Feature flag: saját flag, függ az [[Események]] flagtől; kikapcsolva a fejléc belépő rejtve és az egyeztetés nem fut (`UI/UX elvárások` / `Frontend` flag registry) | Implemented | `feature-flags.service.ts:20,42,58` — `feladatok.googleExport` kulcs + `['feladatok.googleExport', 'feladatok.esemenyek']` függőség-él; `feature-flags.service.spec.ts:105–106` teszteli a két-lépcsős láncot. A „belépő rejtve / egyeztetés nem fut" vákuum-igaz: nincs belépő és nincs egyeztetés | — |
| 17 | Az export nem érinti a saját backendet: nincs új entitásmező, nincs OpenAPI, nincs migráció; az export-állapot device-local; a `calendar_event` sorok változatlanok (`Megjegyzések` / `Architektúra/Backend`) | Holds (verified) | `grep -ri "google\|oauth\|pkce\|gcal" backend/src` → 0 találat | — |
| 18 | Későbbi kör lehet: import (a Google mint új [[Naptár]] producer) és a naplók / esedékességek exportja — egyik sem igényli a terv átírását (`Megjegyzések`) | Describes-future | Explicit jövőbeli scope, nincs kód | — (lefedi: #1) |

## Rollup

- Állítások összesen: 18 — Implemented 2 (#2 flag-érték, #16 flag + függőség-él) / Holds-verified 1 (#17 nincs backend-érintettség) / Describes-future 15
- Blokkoló eltérés: a spec `Státusz: Kész`, de a flagen kívül nulla implementáció van — a teljes `## Architektúra` szakasz jövő idejű. Ez a **dokumentált kivétel** az SSOT-váltáshoz: a `Kész` itt szándékosan „a terv elfogadott és implementálható" értelmű (a spec `**Nem MVP:**` bekezdése és a `documentation/Features/Események.md` + `Naptár.md` „Nem scope (MVP)" sorai ezt megerősítik).
- Draft jegyek: none (lefedi: #1 `backlog/001-google-calendar-export.md`, status `deferred`, flag `feladatok.googleExport`, felhasználói döntés 2026-09-02: egyelőre nem tervezett, később újranyitható)
- Spec-átírás vázlat: **(a) ajánlott** — `documentation/Subfeatures/Google Calendar szinkronizálása.md` `Státusz` `Kész` → `Váz`, a fájl tetejére `> Tervezett: [[backlog/001-google-calendar-export]]` banner, a törzs redukálva „nincs implementálva; tervezett — lásd #1 jegy"-re, a jelenlegi részletes terv megőrizve a jegy hivatkozott design-doc mellékleteként vagy a `### Jelenlegi működés` alá összecsukva. (Alternatíva (b): marad `Kész`, de a törzs explicit „design doc, implementációt a #1 jegy követi" keretezéssel — kevésbé ajánlott, mert a `Kész` a repóban másutt kódkészséget jelent.) Az (a) rögzítendő az audit dokumentált kivételeként az SSOT-flaghez.
- Verdikt: RED (elfogadott, dokumentált kivétel — spec → Váz + banner, ticket #1)
