# Audit — Chunk 08: Auth + Profile + Nyelv + Téma
Audit commit: `ff23984`
Specek: `documentation/Features/Bejelentkezés.md`, `documentation/Features/Profile.md`, `documentation/Features/Nyelv választás.md`, `documentation/Features/Dark&Light mode.md`
Kód: backend `hu/bumler/lm2/auth/**` (JWT, refresh token, admin API, Spring Security), `hu/bumler/lm2/profile/**` (Profile + WeightHistory + sync loaders), `hu/bumler/lm2/common/{GlobalExceptionHandler,CurrentUser}.java`, `db/migration/V2__auth.sql`, `V3__profile.sql`, `openapi/paths/{auth-*,admin-*,profile,weight-history*}.yaml` + `openapi/components/schemas/{Auth*,Admin*,Login*,Refresh*,UserProfile,WeightHistoryEntry}.yaml`; frontend `core/session/**` (auth-session, auth.interceptor, auth.guard, token-refresh-coordinator, jwt), `core/config/{theme,language}.service.ts`, `core/data/{profile,weight-history}.repository.ts`, `core/storage/local-database.service.ts`, `pages/login/**`, `pages/menu/{menu.page,profile,theme,language}/**`, `main.ts`, `app.routes.ts`, `src/global.scss`, `src/theme/variables.scss`, `src/assets/i18n/{hu,en}.json`
Tesztek: backend `auth/AuthAndProfileFlowTests` (MockMvc+Testcontainers e2e), `profile/{ProfileServiceTest,WeightHistoryServiceTest,CrossUserOwnershipIntegrationTest}`; frontend `core/session/{auth-session,auth.interceptor,token-refresh-coordinator}.spec.ts`, `pages/login/login.page.spec.ts` — **nincs** `theme.service.spec.ts` / `language.service.spec.ts` (kód közvetlenül olvasva)

## documentation/Features/Bejelentkezés.md
| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték | Teendő |
|---|---|---|---|---|
| 1 | User-owned adat user-enként elkülönítve; shared katalógus minden bejelentkezettnek (Jelenlegi működés) | Implemented | `CurrentUser.id()` SecurityContext; profile/weight service-ek `userId`-ra szűrnek; `SyncChangesRepository` `user_id = ? OR user_id IS NULL`; `CrossUserOwnershipIntegrationTest` | — |
| 2 | Nincs in-app regisztráció; új user csak admin API-val (Regisztráció — nincs UI) | Implemented | csak `POST /api/admin/users` (`AdminController`); nincs signup route a `app.routes.ts`-ben | — |
| 3 | Tartós session: app-frissítés / újratelepítés nem léptet ki; nincs idle timeout / forced re-login (Session) | Implemented | tokenek `SecureStorage`-ban (`auth-session.service.ts`); nincs timeout-kód; refresh rotáció 180 nap | — |
| 4 | `User.id` UUID, szerver generálja, **nem** offline entitás (User entitás) | Implemented | `AdminUserService.createUser` `UUID.randomUUID()`; `User.java` no IDENTITY; `V2__auth.sql` komment | — |
| 5 | `username` kötelező, case-sensitive, egyedi, 3–32, `^[a-zA-Z0-9._-]{3,32}$` (User entitás) | Implemented | `AdminCreateUserRequest.yaml` `pattern`+min/max → generált jakarta `@Pattern`; `idx_users_username` (case-sensitive); `findByUsername` nincs ignoreCase | Megjegyzés: DB oszlop `varchar(32)`, nincs DB-szintű charset/min CHECK; `LoginRequest` nincs pattern-elve (elfogadható) |
| 6 | `passwordHash` csak szerveren, soha API válaszban / kliens modellben (User entitás) | Implemented | `AdminUser.yaml` nincs password mező; egyetlen séma sem exponálja | — |
| 7 | `role` mindig `USER` (User entitás) | Implemented | `User` konstruktor hardcode `"USER"`, nincs setter; `AdminUser.RoleEnum` egyetlen érték | — |
| 8 | `createdAt`/`updatedAt` audit (User entitás) | Implemented | `users_set_updated_at` trigger + `@Generated` | — |
| 9 | Nincs email / display name / avatar / telefon (User entitás) | Implemented | `users` séma + `AdminUser` DTO csak `id/username/role/createdAt/updatedAt` | — |
| 10 | Nincs „elfelejtett jelszó" flow; jelszócsere/reset csak admin API (Elfelejtett / új jelszó) | Implemented | `PUT /api/admin/users/{username}/password`; nincs self-service reset endpoint | — |
| 11 | Login UI: username+password+gomb; siker → tokenek tárolása → tabok; hibás credential általános üzenet (Bejelentkezés UI) | Implemented | `login.page.html` / `login.page.ts`; `AuthService.login` azonos „Invalid username or password" mindkét ágra → `UNAUTHORIZED`; UI `LOGIN.ERROR` | — |
| 12 | Kijelentkezés UI: Menü → Kijelentkezés, megerősítő dialógus; eszköz refresh revoke szerveren (ha elérhető); helyi tokenek törlődnek; login képernyő (Kijelentkezés UI) | Implemented | `menu.page.ts` `logout()` `AlertController` (+ pending-count figyelmeztetés); `AuthSessionService.logout` előbb `clear()`, majd best-effort `authApi.logout(...).subscribe({error})` | — |
| 13 | Backend-offline / Full-offline kijelentkezés: helyi tokenek azonnal törlődnek, szerver-revoke best-effort (Kijelentkezés UI) | Implemented | `logout()` sorrend: `clear()` majd fire-and-forget subscribe hibaelnyeléssel | — |
| 14 | Access token lejárat → háttérben csendes refresh (Session) | Partial | refresh **csak reaktív** 401-re (`auth.interceptor.ts`); nincs proaktív lejárat-előtti frissítés; `accessTokenExpiresSoon()` definiált, de **sehol nem hívott** (holt kód). Felhasználó számára így is átlátszó | jegy: proaktív csendes refresh |
| 15 | Szándékos kiléptetések: explicit logout, admin jelszócsere → összes token revoke, refresh érvénytelenítés (Session) | Implemented | `AdminUserService.setPassword` minden élő tokent revokál; `AuthService.refresh` rotál (régi `revoke()`) | — |
| 16 | Több eszköz: ugyanaz a User párhuzamosan; eszközönként külön refresh token; egyik logout nem dobja a többit (Több eszköz) | Implemented | `refresh_token` egy sor/session; `logout` csak `tokenHash` szerint revokál; `AuthAndProfileFlowTests.logout_revokesRefreshToken` | — |
| 17 | Refresh token: hosszú élettartam, szerveren hashelve, eszközönként külön sor (`userId`, `tokenHash`, `createdAt`, `expiresAt`, `revokedAt`, opc. `deviceLabel`) (Stack / mechanizmus) | Implemented | `RefreshToken.java` + `V2__auth.sql`; `hash()` = SHA-256 base64, nyers token csak válaszban | Megjegyzés: `device_label` oszlop soha nincs kitöltve (spec: opcionális) |
| 18 | Jelszócsere → az adott user **összes** refresh tokenje revoke (Elfelejtett / új jelszó) | Implemented | `AdminUserService.setPassword` `findByUserIdAndRevokedAtIsNull(...).forEach(revoke)` | jegy: teszt-lefedettség hiányzik |
| 19 | Login form: username auto-focus; jelszó maszkolt, szem-ikon toggle; minden belépéskor újra maszkolt; nincs regisztráció link (UI/UX) | Implemented | `login.page.html` `[autofocus]="true"`, `passwordVisible` signal default `false`, eye toggle; nincs signup link | — |
| 20 | Menü: Kijelentkezés tétel (ha a Bejelentkezés feature be van kapcsolva) (UI/UX) | Implemented | `menu.page.html` feltétel nélküli `(click)="logout()"` tétel | — |
| 21 | Auth guard / interceptor: védett route csak bejelentkezve (Frontend) | Implemented | `authGuard` a `/tabs`-on → `parseUrl('/login')`; `authInterceptor` `/api`-ra Bearer | — |
| 22 | Generált kliens: `Authorization: Bearer`; 401 → refresh → retry; refresh fail → login (Frontend) | Partial | Bearer + 401→`coordinator.refresh()`→retry OK (`auth.interceptor.ts`, `token-refresh-coordinator.service.ts` single-flight); **de** refresh-hiba után csak `authSession.clear()`, nincs aktív `/login` navigáció — csak a következő guardolt navigációnál lép életbe | jegy: refresh-hiba → aktív login redirect |
| 23 | Token tárolás platform secure storage-ban; app update után megmarad (Frontend) | Implemented | `@aparajita/capacitor-secure-storage` (`KEY_ACCESS_TOKEN`/`KEY_REFRESH_TOKEN`) | Megjegyzés: web a plugin web-fallbackját használja (nem httpOnly cookie); web amúgy is online-only |
| 24 | Helyi SQLite/store userId-hoz kötött; előző user adatai ne keveredjenek (izolált DB vagy wipe) (Frontend) | Implemented | `local-database.service.ts` `open(userId)` → külön fájl `lm2_<userId>`; `main.ts` + `login.page.ts` `localDb.open(userId)` | — |
| 25 | Új login (credential) saját backendet igényel; offline → hiba / „nincs kapcsolat" (Backend-offline) | Implemented | `AuthSessionService.login` → `authApi.login` (hálózat); `login.page.ts` `error.status === 0` → `NETWORK` kulcs (külön az érvénytelen credential-től) | — |
| 26 | Auth tokenek **nem** részei az entitás-outboxnak (külön auth session store) (Backend-offline) | Implemented | `AuthSessionService` `SecureStorage`; `clear()` komment: DB + outbox szándékosan érintetlen (§12) | — |
| 27 | Spring Security + JWT; access token 15–60 perc, payload `sub`=userId, `username`, `role=USER` (Backend) | Implemented | `SecurityConfig` `@EnableWebSecurity` STATELESS; `JwtService` TTL 30 perc, claims `sub`/`username`/`role=USER` | — |
| 28 | Refresh token szerveren hashelve; „gyakorlatilag amíg revoke / jelszócsere"; opcionális rotáció (Backend) | Accepted-limitation | `hash()` SHA-256; TTL **fix 180 nap** + minden refresh rotál → aktív eszköz gyakorlatilag nem jár le; a kód-komment ezt tudatosan vállalja | — |
| 29 | Publikus `POST /auth/login`, `/auth/refresh`; védett minden üzleti API; admin: külön api-key header (Backend) | Implemented | `SecurityConfig` `permitAll` lista (`/api/health`, `/api/auth/login`, `/api/auth/refresh`), `anyRequest().authenticated()`; `AdminApiKeyFilter`; `openapi.yaml` `security: []` a két auth path-on | — |
| 30 | Jelszó erős hash (BCrypt/Argon2), plaintext soha nem naplózandó (Backend) | Implemented | `BCryptPasswordEncoder` bean; nincs nyers jelszó logolás | — |
| 31 | Szerep csak `USER`; nincs `ADMIN` role; `@PreAuthorize("isAuthenticated()")` (Authorizáció) | Implemented | `JwtAuthenticationFilter` `ROLE_USER` grant; védelem `authorizeHttpRequests().anyRequest().authenticated()` (funkcionálisan ekvivalens, `@PreAuthorize` annotáció nincs) | — |
| 32 | User-owned: minden query/mutáció `userId`-ra szűr; idegen `id` → 404 (nem 403) (Authorizáció) | Implemented | `ProfileService.upsert`/`newEntity` idegen id → `EntityNotFoundException`; `WeightHistoryService.requireOwner`; `CrossUserOwnershipIntegrationTest` (PUT/POST/DELETE mind 404) | — |
| 33 | Admin: `X-Admin-Api-Key` a `/api/admin/**` alatt, nem JWT role (Authorizáció) | Implemented | `AdminApiKeyFilter` (`MessageDigest.isEqual` konstans-idő); `SecurityConfig` `/api/admin/**` `permitAll` + a filter | — |
| 34 | `POST /api/auth/login` → `{accessToken, refreshToken, expiresIn}` (Auth végpontok) | Implemented | `AuthTokens.yaml`; `AuthService.issueTokens` `accessTokenTtlSeconds()`; teszt `adminCreatesUser_thenLoginSucceeds…` | — |
| 35 | `POST /api/auth/refresh` → új access (+ opc. refresh rotáció) (Auth végpontok) | Implemented | `AuthService.refresh` mindig rotál; `refreshRotatesToken_andRevokedTokenIsRejected` | — |
| 36 | `POST /api/auth/logout` (Bearer) → aktuális refresh revoke (Auth végpontok) | Implemented | `AuthController.logout` → `authService.logout`; `auth-logout.yaml` `bearerAuth`; `logout_revokesRefreshToken` | — |
| 37 | `POST /api/admin/users` → `201` + user DTO **jelszó nélkül**; ütköző username `409` (Admin curl) | Implemented | `AdminController.createUser` `status(201)`; `AdminUserService` `UniqueViolationException` → `409 UNIQUE_VIOLATION`; `duplicateUsername_isRejectedWith409` | — |
| 38 | `PUT /api/admin/users/{username}/password` → `204` (v. `200`); minden eszköz újra bejelentkezik; nincs user → `404` (Admin curl) | Implemented | `AdminController.setUserPassword` `noContent()`; `EntityNotFoundException` → `404 NOT_FOUND`; token-revoke lásd #18 | — |
| 39 | Ownership mátrix — device-local: nyelv, téma, értesítés-kapcsolók nem syncelnek a profilba az első körben | Implemented / Describes-future | `language.service.ts` / `theme.service.ts` `@capacitor/preferences`; profil-sync = **#7** (`backlog/007-profil-szintu-beallitas-sync.md`) | ref #7 |
| 40 | Hibás credential nem árulja el, melyik rossz (Bejelentkezés UI) | Implemented | `AuthService.login` mindkét ág `UnauthorizedException("Invalid username or password")` | — |

## documentation/Features/Profile.md
| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték | Teendő |
|---|---|---|---|---|
| 1 | Felhasználónként **egy** profilrekord (Jelenlegi működés / Entitás) | Implemented | `idx_user_profile_user_id` unique (V3); `ProfileService.upsert` `findByUserId`; kliens determinisztikus v5 `UserProfile:<userId>` | — |
| 2 | Súlyváltozások súlytörténetben megmaradnak; TDEE mindig a jelenlegi testsúllyal (Jelenlegi működés) | Implemented | `weight_history_entry` tábla; `ProfileRepository.save` súlyváltozáskor history sor; TDEE-input csak `currentWeightKg` (Tápérték kalk. — chunkon kívül) | — |
| 3 | Nincs profil-kitöltöttségi gate; hiányos profil mellett is szabad a navigáció (Jelenlegi működés) | Implemented | nincs szerver-gate (`profile.yaml` komment); `profile` route guard nélkül; nincs redirect | — |
| 4 | `UserProfile.id` UUID, kliens generálja az első mentéskor (Entitás) | Implemented | `ProfileRepository.save` `uuidV5(\`UserProfile:${userId}\`)`; `@Id UUID` no IDENTITY | — |
| 5 | `birthDate` opcionális, ha kitöltve érvényes dátum (Entitás) | Implemented | `UserProfile.yaml` `format: date` nullable; `user_profile.birth_date date` nullable; életkor-számítás chunkon kívül | — |
| 6 | `sex` `MALE`/`FEMALE` ha kitöltve (Entitás) | Implemented | séma enum nullable; DB `CHECK (sex IN ('MALE','FEMALE'))` | — |
| 7 | `heightCm` 100–250 (Entitás) | Implemented | `UserProfile.yaml` min/max; DB `CHECK … BETWEEN 100 AND 250`; `profile.page.ts` `Validators.min/max` | — |
| 8 | `currentWeightKg` 30–300, **max 1 tizedes** (Entitás) | Partial | tartomány: séma + DB `CHECK` + form validátor OK; „max 1 tizedes" csak a DB-oszlop `numeric(5,1)` skálája — nincs explicit kliens-validáció / kerekítés | jegy: 1-tizedes bevitel-validáció kliensen |
| 9 | `goal` `FAT_LOSS`/`MAINTENANCE`/`WEIGHT_GAIN` (Entitás) | Implemented | séma enum + DB CHECK; `GoalEnum` a formban | — |
| 10 | `kgPerWeek` 0.1–1.5; `FAT_LOSS`/`WEIGHT_GAIN` + kitöltött goal → mentéskor kötelező; `MAINTENANCE` → mező rejtett, érték ignorált (Entitás / Mentés validáció) | Implemented | `ProfileService.validate` (goal-függő kötelező); `profile.page.ts` `kgPerWeekRequiredValidator` + `showKgPerWeek` computed + `save()` `MAINTENANCE`-nél `null`-ra állít; `ProfileServiceTest` 4 eset | — |
| 11 | `grossMonthlySalaryHuf` opcionális, egész `≥ 0` (Entitás) | Implemented | `UserProfile.yaml` `int64` min 0 nullable; DB `CHECK >= 0` | — |
| 12 | Nincs `activityLevel` (PAL fix 1.2) (Entitás) | Implemented | nincs mező az entitásban / sémában; `ProfileService` komment | — |
| 13 | Nincs display name / avatar / email (Entitás) | Implemented | `user_profile` séma nem tartalmazza | — |
| 14 | Üres / részleges profil menthető (és üresen létezhet helyi store-ban) (Mentés validáció) | Implemented | `ProfileService.validate` csak `kgPerWeek`-et nézi; `upsert_doesNotRequireKgPerWeek_whenGoalIsAbsent`; SQLite `user_profile` minden mező nullable | — |
| 15 | `WeightHistoryEntry`: `id` UUID kliens; `recordedAt` kliens TZ, default = rögzítés pillanata; `weightKg` 30–300; `deleted` soft delete default false, listák szűrik (Súlytörténet) | Implemented | `WeightHistoryEntry.yaml` + `V3`; `weight-history.repository.ts` `add` `uuidV4()` + `new Date().toISOString()`; `list` → `findByUserIdAndDeletedFalseOrderByRecordedAtDesc` | — |
| 16 | Írás Profile mentéskor: `currentWeightKg` **változott** (és kitöltött) → új history sor; más mező változása nem nyit sort; első súly (üres→érték) → egy sor (Súlytörténet) | Implemented | `ProfileRepository.save`: `previousWeight` = korábbi `profile()?.currentWeightKg`; `if (nextWeight !== null && nextWeight !== previousWeight) weightHistory.add(...)` | — |
| 17 | Manuális „+ Új bejegyzés" CTA: közvetlen history sor tetszőleges `recordedAt`+`weightKg`-val (retroaktív); ez a create út **nem** módosítja a `currentWeightKg`-t (Súlytörténet) | Implemented | `profile.page.ts` `startAddEntry`/`saveEntry` → `weightHistoryRepository.add(iso, kg)`; teljesen külön a profile formtól | — |
| 18 | History CRUD: create / edit / delete (soft delete + megerősítés); soha nem syncelt draft → helyi hard remove + outbox tisztítás; nem írja át a `currentWeightKg`-t; listák `deleted=false`; nincs undelete UI (Súlytörténet / CRUD) | Implemented | `WeightHistoryService` (update `ENTITY_DELETED` ha törölt; delete idempotens soft); `profile.page.ts` `deleteEntry` `AlertController`; hard-remove-of-draft a storage backend generikus útján (nem chunk-részletezve) | — |
| 19 | Belépés: Menü → Profile (UI/UX) | Implemented | `menu.page.html` `routerLink="profile"`; `app.routes.ts` `menu/profile` | — |
| 20 | Egy űrlap; Mentés gomb; nincs élő TDEE előnézet ezen a képernyőn (UI/UX) | Implemented | `profile.page.ts` — nincs TDEE import/számítás | — |
| 21 | Mentés után rövid siker-feedback; store frissül → más képernyők TDEE-je újraszámol (UI/UX) | Implemented | `savedRecently` signal 2 mp; `profileRepository.profile` signal frissül | — |
| 22 | Súlytörténet lista a Profile képernyőn; „+ Új bejegyzés"; szerkesztés/törlés; **diagram nincs** az első körben (UI/UX) | Implemented | `profile.page.html` lista + inline entry form; nincs chart komponens | — |
| 23 | OpenAPI: `UserProfile` (1:1) + `WeightHistoryEntry` CRUD user scope; history `deleted`/`deleted_at`; `DELETE` = soft delete, idempotens (Backend) | Implemented | `profile.yaml`, `weight-history*.yaml`; `weightHistory_isIdempotentAndSoftDeletable` (dupla delete → 200, deleted=true) | — |
| 24 | `GET /api/profile` → `404` az első `PUT`-ig (Backend) | Implemented | `ProfileService.get` → `EntityNotFoundException` → 404; `profileUpsert_validatesKgPerWeek…` első lépése | — |
| 25 | Saját törölt weight sor `GET`-je → `200` `deleted=true` (nem 404) (Backend / global error-shape) | Implemented | `WeightHistoryService.get` `findByIdAndUserId` deleted-szűrés nélkül → mapper `deleted=true` | jegy: nincs teszt erre az ágra |
| 26 | Idempotens upsert kliens id-n; `PUT` törölt weight soron → `409 ENTITY_DELETED` (Backend / offline) | Implemented | `WeightHistoryService.create`/`update`; `weightHistory_isIdempotentAndSoftDeletable` (`updateAfterDelete` → 409 `ENTITY_DELETED`) | — |
| 27 | Profile + súlytörténet olvasás/írás helyi store-ból Backend-offline és Full-offline esetén (Backend-offline) | Implemented | `SqliteStorageBackend`; `SCHEMA_V1` `user_profile` + `weight_history_entry` táblák | — |
| 28 | Create/update/delete → outbox (`OfflineQueueService`) + kliens UUID; sync (Backend-offline) | Implemented | `profile.repository.ts` / `weight-history.repository.ts` `requestDrainDebounced()` natívon; storage backend enqueue | — |
| 29 | Sync / pull hiba **nem** törölheti a helyi profilt és history-t (Backend-offline) | Partial | design-elv; a chunkban közvetlen bizonyíték/teszt nem ellenőrizve (SyncEngine chunkon kívül) | — |
| 30 | Nincs szerveroldali „profile complete" gate (Backend) | Implemented | `ProfileService.upsert` csak `kgPerWeek` feltételes ellenőrzés | — |

## documentation/Features/Nyelv választás.md
| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték | Teendő |
|---|---|---|---|---|
| 1 | Kétnyelvű (hu + en); default a készülék nyelvét követi, **hu-ra esik vissza ha nem támogatott** (Jelenlegi működés) | Partial | `language.service.ts` `systemLanguage()` = `navigator.language.startsWith('hu') ? 'hu' : 'en'` → bármely nem-hu locale (pl. `de`) **`en`**-t ad, nem `hu`-t | jegy: ismeretlen locale → `hu` fallback |
| 2 | Három állapot: Rendszer (készülék nyelve ha hu/en, **egyébként hu**) / Magyar (fix hu) / Angol (fix en) (Funkcionális leírás) | Partial | fix `hu`/`en` OK (`mode` signal); a Rendszer „egyébként hu" ága hibás (lásd #1) | (mint #1) |
| 3 | Tárolás device-local (`@capacitor/preferences`), nem syncel profilba / más eszközre az első körben (Funkcionális leírás) | Implemented / Describes-future | `Preferences` kulcs `lm2_language`; profil-sync = **#7** | ref #7 |
| 4 | A váltás azonnali, app-újraindítás nélkül (Funkcionális leírás) | Implemented | `setMode` → `translate.use(activeLanguage())` + signal frissül | — |
| 5 | Fordítófájlok build assetek, Full-offline is elérhetők (Funkcionális leírás) | Implemented | `src/assets/i18n/{hu,en}.json`; `provideTranslateHttpLoader({ prefix: '/assets/i18n/' })` | — |
| 6 | i18n alá tartozik minden user-facing szöveg: enum/állapot címkék, értesítés-szövegek, hibaüzenetek (`code` alapján), üres állapotok, dialógusok, `~` magyarázatok (Mi tartozik az i18n alá) | Implemented | login/menu/theme/language/profile lapok `TranslatePipe`/`translate.instant`; `{count}` paraméteres kulcsok (`MENU.LOGOUT_CONFIRM_PENDING`); szerver-`code`→kulcs a kliens hibarétegben (chunkon kívüli lapokon részleges) | — |
| 7 | Dátum- és számformázás a **kiválasztott nyelv** locale-ját követi (`hu`, ill. `en-GB`), nem a készülékét; csak megjelenítés (Formázás) | Missing | nincs `registerLocaleData` / `LOCALE_ID` sehol; `profile.page.ts` `DatePipe` alap `en-US` locale-lal fut; a nyelvváltás nem állítja az Angular locale-t | jegy: `LOCALE_ID`/`registerLocaleData` a kiválasztott nyelvhez |
| 8 | Szerver-hibák: kliens a `code` alapján fordít, a szerver `message`-e csak fallback/diagnosztika; új hibakód = új i18n kulcs mindkét nyelvben (Szerver-hibák fordítása) | Implemented (szerver-kontraktus) | `GlobalExceptionHandler` minden ágon stabil `code` (`UNAUTHORIZED`, `VALIDATION_ERROR`, `UNIQUE_VIOLATION`, `ENTITY_DELETED`, `NOT_FOUND`, …); `message` csak diagnosztika | — |
| 9 | Nyelvváltás után az ütemező újraértékel és újraütemez (a régi nyelvű ütemezések miatt) (Nyelvváltás és a már ütemezett értesítések) | Implemented | `notification-scheduler.service.ts` `effect()` olvassa `this.language.activeLanguage()` → debounced reconcile; baked-nyelv eltérésnél cancel+reschedule (spec-teszt `notification-scheduler.service.spec.ts:237`) | — |
| 10 | Hiányzó kulcs: fallback `hu`; ha ott sincs, a kulcs jelenik meg (nem üres); dev buildben konzol-figyelmeztetés (Hiányzó kulcs) | Partial | `provideTranslateService({ fallbackLang: 'hu' })` → hu-ra esik, majd kulcs; **de** nincs `MissingTranslationHandler` és **nincs** dev konzol-warning | jegy: központi `MissingTranslationHandler` + dev warning |
| 11 | `hu.json` és `en.json` kulcshalmaza azonos kell legyen; eltérés = build / CI hiba (Hiányzó kulcs) | Partial | jelenleg paritásban (1015 ↔ 1015 kulcs, nulla eltérés) — **de nincs** paritás-ellenőrző script/teszt / CI-lépés | jegy: kulcs-paritás build-idejű ellenőrzés |
| 12 | Belépés: Menü → Nyelv (`/tabs/menu/language`) (UI/UX) | Implemented | `menu.page.html` `routerLink="language"`; `app.routes.ts` `menu/language` guard nélkül | — |
| 13 | Három rádió opció, azonnal érvényes, nincs „mentés" gomb, nincs újraindítás-kérés (UI/UX) | Implemented | `language.page.html` `ion-radio-group` `(ionChange)="setMode(...)"` | — |
| 14 | A nyelvek neve a saját nyelvén (Magyar / English) (UI/UX) | Implemented | `language.page.html` hardcode „Magyar" / „English" (nem `translate`) | — |
| 15 | Core feature: nincs feature flagje, nem kapcsolható ki (UI/UX) | Implemented | route guard nélkül; menü tétel feltétel nélkül | — |
| 16 | Nincs ICU plural; paraméterezett `{{count}}` kulcs (Megjegyzések) | Implemented | pl. `MENU.LOGOUT_CONFIRM_PENDING` `{ count }` interpolációval | — |
| 17 | Támogatott nyelvek listája egy helyen él (Megjegyzések) | Partial | `LanguageMode`/`ActiveLanguage` típus + `systemLanguage()` + `main.ts` `provideTranslateService` — néhány helyre szórva, nincs egyetlen `SUPPORTED_LANGUAGES` konstans | (kis súly) |
| 18 | ngx-translate; `assets/i18n/hu.json` + `en.json`; egymásba ágyazott `feature.képernyő.elem` kulcsok (Architektúra Frontend) | Implemented | `provideTranslateService` + `provideTranslateHttpLoader`; JSON beágyazott struktúra | — |
| 19 | `LanguageService` (root, `core/config/`): beállítás signal, rendszernyelv feloldás, `TranslateService.use`, **Angular locale (`registerLocaleData`, `LOCALE_ID`)**, natív értesítés-újraütemezés kiváltása (Architektúra Frontend) | Partial | signal + feloldás + `use()`: megvan; `registerLocaleData`/`LOCALE_ID`: **hiányzik**; értesítés-újraütemezés megvan, de a schedulertől figyelve, nem a `LanguageService` váltja ki | (lásd #7 jegy) |
| 20 | Nyelv betöltése az app init része (a témával együtt, hálózat nélkül) (Architektúra Frontend) | Implemented | `main.ts` `provideAppInitializer` `await Promise.all([languageService.init(), themeService.init(), …])` | — |
| 21 | Hiányzó kulcs kezelése központi `MissingTranslationHandler`-ben, nem komponensenként (Architektúra Frontend) | Missing | nincs `MissingTranslationHandler` implementáció / provider | (mint #10) |
| 22 | Kulcs-paritás ellenőrzés script/teszt, a build része (Architektúra Frontend) | Missing | nincs ilyen script/teszt/CI lépés a `frontend/`-ben | (mint #11) |
| 23 | Backend: nincs backend érintettség a szövegekre; egy követelmény: minden hibaválasz stabil `code`-ot ad (Backend) | Implemented | `GlobalExceptionHandler` egy `@RestControllerAdvice`, minden ág `ApiError(code, …)` | — |

## documentation/Features/Dark&Light mode.md
| # | Spec-állítás (rövid + szekció) | Verdikt | Bizonyíték | Teendő |
|---|---|---|---|---|
| 1 | Dark + light támogatás; default a készülék rendszertémáját követi; user felülírhatja fix világos/sötétre (Jelenlegi működés) | Partial | Rendszer-követés megy (`dark.system.css` media query + `ThemeService` media listener); **fix felülírás nem érvényesül** — lásd #7/#8 | (jegy #7 lent) |
| 2 | Három állapot: Rendszer (`prefers-color-scheme`-et követ, **élőben** reagál futásidejű OS-váltásra) / Világos (fix) / Sötét (fix) (Funkcionális leírás) | Partial → RED | Rendszer élő reakció OK (`media.addEventListener('change', …)` + `dark.system.css`). **Fix Világos sötét készüléken NEM kényszerít világosat** (a `dark.system.css` media query továbbra is nyer), **Fix Sötét világos készüléken NEM kényszerít sötétet** (az `ion-palette-dark` osztálynak nincs mögöttes CSS-e) | **jegy (blokkoló): `dark.class.css` bekötése** |
| 3 | Tárolás device-local (`@capacitor/preferences`), nem syncel más eszközre az első körben (Funkcionális leírás) | Implemented / Describes-future | `theme.service.ts` `Preferences` kulcs `lm2_theme`; profil-sync = **#7** | ref #7 |
| 4 | A váltás azonnali, app-újraindítás nélkül (Funkcionális leírás) | Implemented | `setMode` → `apply()` szinkron (osztály-toggle + `StatusBar.setStyle`) + `Preferences.set` | — |
| 5 | Nincs saját időzített „napnyugtakor sötét"; az OS adja, a Rendszer mód átveszi (Funkcionális leírás) | Implemented | csak `matchMedia('(prefers-color-scheme: dark)')` listener | — |
| 6 | Kontraszt: szöveg ≥ 4.5:1, nagy szöveg/ikon ≥ 3:1 mindkét palettában (WCAG AA) (Kontraszt) | Accepted-limitation | nincs automatikus kontraszt-ellenőrzés; `variables.scss` üres → az Ionic alap-palettáira támaszkodik | — |
| 7 | Komponensekben nincs hardcode színérték; minden szín téma-tokenből (`--ion-color-*`, saját szemantikus tokenek) (Kontraszt) | Partial | nincs központi szemantikus token-készlet; `theme/variables.scss` gyakorlatilag üres (csak komment); komponens-szintű ellenőrzés e chunkon kívül | jegy: központi szemantikus szín-tokenek |
| 8 | Szemantikus állapotszínek mindkét palettában **külön** hex (sync szürke/piros, Étkezés progress skála, `_dirty`/`_sync_error`) (Kontraszt) | Partial | nincs `variables.scss`-beli per-paletta felülírás; az érintett specek saját tokenjei nincsenek téma-szinten definiálva | (mint #7) |
| 9 | A jelentés soha nem áll csak színen (ikon + szöveg is) (Kontraszt) | Accepted-limitation | az érintett specek (Szinkr. központ, Étkezés) követelménye; e chunkban nem auditált | — |
| 10 | Status bar szöveg-/háttérstílus a témához igazodik (`@capacitor/status-bar`) (Rendszer-chrome) | Implemented | `ThemeService.apply()` → `StatusBar.setStyle({ style: dark ? Dark : Light })` (csak natív) | Megjegyzés: csak `setStyle`, nincs `setBackgroundColor` |
| 11 | Splash / app háttérszín témánként definiált, hogy induláskor ne legyen fehér villanás (Rendszer-chrome) | Missing | nincs `SplashScreen` `backgroundColor` konfiguráció; `variables.scss`/`index.html` nem definiál témánkénti hátteret (csak `<meta name="color-scheme">`) | jegy: témánkénti splash / háttérszín |
| 12 | A téma beállítása az indulási sorrend korai lépése, még az első képernyő előtt (Rendszer-chrome) | Implemented | `main.ts` `provideAppInitializer` `await themeService.init()` a bootstrap feloldása előtt | — |
| 13 | Belépés: Menü → Téma (`/tabs/menu/theme`) (UI/UX) | Implemented | `menu.page.html` `routerLink="theme"`; `app.routes.ts` `menu/theme` guard nélkül | — |
| 14 | Három rádió opció Rendszer / Világos / Sötét i18n szövegekkel (UI/UX) | Implemented | `theme.page.html` `ion-radio` + `TranslatePipe` (`THEME.SYSTEM/LIGHT/DARK`) | — |
| 15 | A választás azonnal látszik a beállítás képernyőn is (nincs „mentés" gomb) (UI/UX) | Implemented | `[value]="mode()"` kötés + `apply()` azonnali | — |
| 16 | Core feature: nincs feature flagje, nem kapcsolható ki (UI/UX) | Implemented | route guard nélkül; menü tétel feltétel nélkül | — |
| 17 | Ionic sötét paletta dokumentált **osztály-kapcsolóval** a gyökér elemen (`ion-palette-dark`); Rendszer mód ezt állítja/veszi le `prefers-color-scheme` változásra (Megjegyzések) | Partial → RED | `ThemeService.apply()` `classList.toggle('ion-palette-dark', dark)`, **de** `global.scss` az `@ionic/angular/css/palettes/dark.system.css`-t importálja (media-query stratégia), a `dark.class.css` **ki van kommentelve** → az osztály hatástalan | (mint #2) |
| 18 | A konkrét CSS változó-készlet a `theme/variables.scss`-ben él (Megjegyzések) | Missing | `src/theme/variables.scss` gyakorlatilag üres (2 sor komment) | (mint #7) |
| 19 | `ThemeService` (root, `core/config/`): beállítás signal, `prefers-color-scheme` figyelés, osztály-kapcsoló + status bar stílus (Architektúra Frontend) | Partial | signal + media figyelés + status bar OK; az osztály-kapcsoló önmagában hatástalan (#17) | (mint #2) |
| 20 | A preferencia betöltése az app init része (téma + nyelv együtt, hálózat nélkül) (Architektúra Frontend) | Implemented | `main.ts` `provideAppInitializer` `Promise.all([… themeService.init() …])` | — |
| 21 | Nincs komponens-szintű téma logika; a komponensek CSS változókat használnak, a service csak a gyökér osztályt állítja (Architektúra Frontend) | Partial | a service a gyökér osztályt állítja, de semmi nem fogyasztja; a komponensek az Ionic alap-palettáira támaszkodnak | (mint #7) |
| 22 | Backend: nincs backend érintettség (Backend) | Implemented | nincs téma-kód a backendben | — |

## Rollup
- Állítások összesen: **115** — Implemented **83** / Partial **20** / Missing **5** / Describes-future **1** (a 3 device-local sor #7-tel jelölve, elsődlegesen Implemented-ként számolva) / Accepted-limitation **6**
- Blokkoló eltérések:
  - **Dark&Light #2 / #17 (RED):** a `global.scss` csak a `dark.system.css`-t importálja, a `dark.class.css`-t nem. Így a `ThemeService` `ion-palette-dark` osztály-kapcsolása hatástalan: **fix „Világos" sötét készüléken nem világosít**, **fix „Sötét" világos készüléken nem sötétít**. Egy core, ki-nem-kapcsolható feature elsődleges user-akciója nem működik, ha a rendszertéma eltér.
- Draft jegyek:
  - bug — „Dark&Light: `dark.class.css` bekötése, hogy a fix Világos/Sötét felülírás érvényesüljön" -> Dark&Light mode — `global.scss` csak `dark.system.css`-t importál; a `ThemeService` osztály-toggle inert, a fix felülírás nem veri a készülék preferenciáját (BLOKKOLÓ).
  - bug — „Rendszer nyelv: ismeretlen locale `hu`-ra essen vissza, ne `en`-re" -> Nyelv választás — `systemLanguage()` bármely nem-hu locale-ra `en`-t ad; a spec `hu` fallbacket ír elő.
  - feature — „Angular locale (`LOCALE_ID` / `registerLocaleData`) a kiválasztott nyelvhez kötése" -> Nyelv választás — nincs locale-regisztráció; a dátum/szám pipe-ok mindig `en-US`; a spec `hu` / `en-GB` megjelenítést vár.
  - feature — „Központi `MissingTranslationHandler` + dev konzol-figyelmeztetés hiányzó kulcsra" -> Nyelv választás — nincs handler; dev buildben nincs hiányzó-kulcs warning.
  - test/ci — „`hu.json` / `en.json` kulcs-paritás build-idejű ellenőrzés" -> Nyelv választás — jelenleg paritásban (1015/1015), de semmi nem kényszeríti; a spec szerint az eltérés build/CI hiba.
  - feature — „Splash / app háttérszín témánként, fehér villanás nélkül" -> Dark&Light mode — nincs témánkénti splash/háttér konfiguráció.
  - feature — „Központi szemantikus szín-tokenek külön light/dark értékkel (`theme/variables.scss`)" -> Dark&Light mode — `variables.scss` üres; nincs per-paletta szemantikus státuszszín; a spec külön hexet és hardcode-mentességet ír elő.
  - enhancement — „Proaktív csendes access-token frissítés lejárat előtt" -> Bejelentkezés — a refresh csak reaktív (401); `accessTokenExpiresSoon()` holt kód; a spec háttér-megújítást ír le.
  - bug — „Refresh-hiba után aktív átirányítás a login képernyőre" -> Bejelentkezés — sikertelen csendes refresh után a session törlődik, de a user csak a következő guardolt navigációnál kerül `/login`-ra.
  - test — „Admin jelszócsere → összes refresh token revoke lefedése teszttel" -> Bejelentkezés — `AdminUserService.setPassword` revokál, de nincs rá teszt.
  - chore — „`ThemeService` / `LanguageService` unit tesztek" -> Dark&Light mode / Nyelv választás — nincs `theme.service.spec.ts` / `language.service.spec.ts` core státusz mellett.
  - enhancement — „`currentWeightKg` / `weightKg` 1-tizedes bevitel-validáció a kliensen" -> Profile — a spec „max 1 tizedes"; a kliens csak min/max-ot validál, a DB `numeric(5,1)` skálájára hagyatkozik.
  - (referált) **#7** `backlog/007-profil-szintu-beallitas-sync.md` — nyelv/téma/értesítés-kapcsolók profil-szintű (több-eszközös) sync-je; nem újraírandó.
- Spec-átírás vázlat:
  - **Bejelentkezés.md:** a `verifikalva` stub kitöltése; a „csendes refresh" mondatot pontosítani „a 401 válasz utáni átlátszó refresh+retry" felé (reaktív, nem proaktív), vagy jegyre hivatkozni; rögzíteni, hogy a refresh TTL fix 180 nap + rotáció (nem szó szerint „örök"); rögzíteni, hogy az endpoint-védelem `authorizeHttpRequests` (nem `@PreAuthorize` annotáció); jelezni, hogy web tokentárolás a secure-storage plugin web-fallbackja (nem httpOnly cookie), és web amúgy is online-only.
  - **Profile.md:** rögzíteni, hogy a „max 1 tizedes" jelenleg csak DB-oszlop-skála (nincs kliens-validáció) — vagy jegyre hivatkozni; jelezni, hogy a súlyváltozás→history-sor logika kliensoldali (`ProfileRepository.save`), a backend nem hozza létre automatikusan.
  - **Nyelv választás.md:** jelen idejűre írni; a „Formázás" szekcióhoz megjegyzés, hogy a `LOCALE_ID`/`registerLocaleData` még nincs bekötve (jegy); a „Hiányzó kulcs" + „kulcs-paritás" pontokhoz megjegyzés, hogy a `MissingTranslationHandler` és a paritás-CI még hiányzik (jegyek); a Rendszer-mód fallback tényleges viselkedése (`en` ismeretlen locale-ra) eltér — jegy.
  - **Dark&Light mode.md:** jelen idejűre írni; a „Megjegyzések" szekcióban a `dark.class.css` vs `dark.system.css` tény rögzítése + BLOKKOLÓ jegyre hivatkozás; a `theme/variables.scss` üres állapotának + a hiányzó splash-háttér és szemantikus tokenek rögzítése (jegyek); a status bar rész pontosítása (`setStyle` igen, `setBackgroundColor` nem).
- Verdikt: **RED** — az Auth és a Profile slice önmagában GREEN (erős e2e + unit lefedettség, a végpont-, ownership- és validációs kontraktus teljesül), de a **Dark&Light mode** fix felülírása nem működik (osztály-stratégia nincs bekötve), és a **Nyelv választás** több kötelező mechanizmusa hiányzik (Angular locale, `MissingTranslationHandler`, kulcs-paritás CI) vagy hibás (ismeretlen-locale fallback).
