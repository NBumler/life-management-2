/**
 * Life Management 2.0 API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { WorkoutExerciseEntry } from './workoutExerciseEntry';


/**
 * documentation/Subfeatures/Edzésnapló.md — one logged strength / HIIT / mászó-kiegészítő session (\"mit csináltál ténylegesen?\"). documentation/Architektúra/Backend.md \"Nested aggregate PUT\": WorkoutSession + its WorkoutExerciseEntry rows + their WorkoutSetEntry rows saved/read as one tree, on every endpoint including the plain list — `exercises` (and each entry\'s `sets`) always lists every row, live or soft-deleted (the client filters `deleted = false` itself when rendering or summing). No kcal / body-weight field: `activityExtraKcal` is a pure client calculation (documentation/Features/Tápérték kalkulátor.md).
 */
export interface WorkoutSession { 
    id: string;
    /**
     * Calendar date in the client\'s TZ — the aznapi activityExtraKcal bucket.
     */
    date: string;
    /**
     * HH:mm, 24h. Optional; independently of endTime.
     */
    startTime?: string | null;
    /**
     * HH:mm, 24h. Must be strictly after startTime when both are present.
     */
    endTime?: string | null;
    /**
     * Manually overridable. Default endTime − startTime, or a stopwatch. Missing → the client estimates (sets × 3 min) for the kcal preview; the server just stores what is sent.
     */
    durationMinutes?: number | null;
    /**
     * MET source — GENERAL_WEIGHTS 5.0, HIIT_CIRCUIT 8.0. Editable after save.
     */
    workoutType: WorkoutSession.WorkoutTypeEnum;
    title?: string | null;
    notes?: string | null;
    location?: WorkoutSession.LocationEnum | null;
    /**
     * → [[Heti terv]] WorkoutPlan.id (static template). Null = ad-hoc.
     */
    planId?: string | null;
    /**
     * HIIT_CIRCUIT rounds count.
     */
    roundsCount?: number | null;
    exercises: Array<WorkoutExerciseEntry>;
    deleted: boolean;
    readonly deletedAt?: string | null;
    readonly createdAt?: string;
    readonly updatedAt?: string;
}
export namespace WorkoutSession {
    export const WorkoutTypeEnum = {
        GeneralWeights: 'GENERAL_WEIGHTS',
        HiitCircuit: 'HIIT_CIRCUIT'
    } as const;
    export type WorkoutTypeEnum = typeof WorkoutTypeEnum[keyof typeof WorkoutTypeEnum];
    export const LocationEnum = {
        HomeGym: 'HOME_GYM',
        CommercialGym: 'COMMERCIAL_GYM',
        OutdoorPark: 'OUTDOOR_PARK'
    } as const;
    export type LocationEnum = typeof LocationEnum[keyof typeof LocationEnum];
}


