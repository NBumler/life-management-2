/**
 * Life Management 2.0 API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


/**
 * documentation/Subfeatures/Rendszeres kiadások.md — one recurring, fixed-amount expense (subscription, pass, insurance). Flat, user-owned CRUD; the name is NOT unique. The monthly equivalent that the Pénzügyek dashboard and the AYCM \"megéri-e\" card read is a pure client utility (monthlyEquivalentHuf) — the server never computes it. The server also never auto-rolls nextBillingDate: \"Fizetve\" is a plain PUT with a client-computed date.
 */
export interface RecurringExpense { 
    id: string;
    /**
     * Required, non-blank after trim. Not unique (two \"Internet\" rows are fine).
     */
    name: string;
    /**
     * Whole HUF, no decimals. The charged amount per billing period (not per month).
     */
    amountHuf: number;
    /**
     * Billing period. monthlyEquivalentHuf divides by 1 / 3 / 12 respectively.
     */
    frequency: RecurringExpense.FrequencyEnum;
    category: RecurringExpense.CategoryEnum;
    /**
     * Client calendar day (YYYY-MM-DD) of the next expected charge. No auto-roll — a past date stays past until the user taps \"Fizetve\" or edits it.
     */
    nextBillingDate: string;
    /**
     * The intended day-of-period (so the 12th stays the 12th; the 31st clamps in short months then restores). Kept in sync with nextBillingDate\'s day on create and on a manual date edit; never touched by \"Fizetve\" / frequency / amount / name / category / notes / active changes.
     */
    billingDayOfMonth: number;
    /**
     * false = paused: the row stays in the list but drops out of the dashboard / AYCM monthly total.
     */
    active: boolean;
    notes?: string | null;
    deleted: boolean;
    readonly deletedAt?: string | null;
    readonly createdAt?: string;
    readonly updatedAt?: string;
}
export namespace RecurringExpense {
    export const FrequencyEnum = {
        Monthly: 'MONTHLY',
        Quarterly: 'QUARTERLY',
        Yearly: 'YEARLY'
    } as const;
    export type FrequencyEnum = typeof FrequencyEnum[keyof typeof FrequencyEnum];
    export const CategoryEnum = {
        Entertainment: 'ENTERTAINMENT',
        Sport: 'SPORT',
        Utilities: 'UTILITIES',
        Insurance: 'INSURANCE',
        Other: 'OTHER'
    } as const;
    export type CategoryEnum = typeof CategoryEnum[keyof typeof CategoryEnum];
}


