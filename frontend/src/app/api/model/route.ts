/**
 * Life Management 2.0 API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


/**
 * documentation/Subfeatures/Outdoor köteles admin.md — a user-owned rope route under a Sector. `guidebookGrade` is the raw guidebook string; the napló parses it, the server stores it verbatim (no matrix index column). `lengthInMeters` / `totalPitches` / `rockType` / `aspect` are optional prefill values for the rope napló — when `rockType` / `aspect` are set they win over the Sector/Crag default. The sector link is fixed at create time.
 */
export interface Route { 
    id: string;
    sectorId: string;
    name: string;
    guidebookGrade: string;
    /**
     * Optional route length, prefills the napló\'s lengthInMeters.
     */
    lengthInMeters?: number | null;
    /**
     * Optional pitch count, prefills the napló.
     */
    totalPitches?: number | null;
    /**
     * Optional per-route rock type; when set it wins over the Sector/Crag default.
     */
    rockType?: string | null;
    /**
     * Optional per-route aspect; when set it wins over the Sector default.
     */
    aspect?: string | null;
    deleted: boolean;
    readonly deletedAt?: string | null;
    readonly createdAt?: string;
    readonly updatedAt?: string;
}

