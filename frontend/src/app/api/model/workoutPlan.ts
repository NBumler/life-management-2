/**
 * Life Management 2.0 API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { WorkoutPlanExercise } from './workoutPlanExercise';


/**
 * documentation/Subfeatures/Heti terv.md — a static training template / routine (\"mit kéne csinálnod?\"). documentation/Architektúra/Backend.md \"Nested aggregate PUT\": WorkoutPlan + its WorkoutPlanExercise rows + their WorkoutPlanSet rows saved/read as one tree, on every endpoint including the plain list — `exercises` (and each exercise\'s `targetSets`) always lists every row, live or soft-deleted (the client filters `deleted = false` itself). `active` is a plain field toggled through the ordinary PUT (no dedicated endpoint); turning it off hides the template from the pickers but never touches past WorkoutSession.planId / WeeklyPlan slots pointing at it.
 */
export interface WorkoutPlan { 
    id: string;
    name: string;
    notes?: string | null;
    /**
     * Default true on create. false = hidden from the slot / \"Terv indítása\" pickers, not deleted.
     */
    active: boolean;
    /**
     * Display-only grouping label in the list / picker (\"Alap rotáció\", \"Cél: …\"). No business logic.
     */
    goalLabel?: string | null;
    /**
     * Optional — pre-fills the session\'s workoutType when a workout is started from this template.
     */
    defaultWorkoutType?: WorkoutPlan.DefaultWorkoutTypeEnum | null;
    exercises: Array<WorkoutPlanExercise>;
    deleted: boolean;
    readonly deletedAt?: string | null;
    readonly createdAt?: string;
    readonly updatedAt?: string;
}
export namespace WorkoutPlan {
    export const DefaultWorkoutTypeEnum = {
        GeneralWeights: 'GENERAL_WEIGHTS',
        HiitCircuit: 'HIIT_CIRCUIT'
    } as const;
    export type DefaultWorkoutTypeEnum = typeof DefaultWorkoutTypeEnum[keyof typeof DefaultWorkoutTypeEnum];
}


