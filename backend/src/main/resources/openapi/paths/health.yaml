get:
  operationId: getHealth
  tags:
    - health
  summary: Availability probe. Public, no DB round-trip; the client uses this to decide BACKEND_OFFLINE.
  security: []
  responses:
    '200':
      description: Backend is reachable.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/HealthResponse.yaml'
