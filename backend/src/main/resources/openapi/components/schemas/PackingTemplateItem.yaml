type: object
description: documentation/Subfeatures/Sablonok.md — a GearItem reference inside a PackingTemplate, own sync entity.
required:
  - id
  - templateId
  - gearItemId
  - sortOrder
  - deleted
properties:
  id:
    type: string
    format: uuid
  templateId:
    type: string
    format: uuid
  gearItemId:
    type: string
    format: uuid
  sortOrder:
    type: integer
  deleted:
    type: boolean
  deletedAt:
    type: string
    format: date-time
    nullable: true
    readOnly: true
  createdAt:
    type: string
    format: date-time
    readOnly: true
  updatedAt:
    type: string
    format: date-time
    readOnly: true
