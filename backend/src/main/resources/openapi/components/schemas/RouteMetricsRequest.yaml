type: object
description: >-
  backlog/tura-utvonaltervezo/103-... 2.3 fázis — táv/idő/szintkülönbség-becslés kérése egy
  megrajzolt vagy automatikusan generált útvonalhoz.
required:
  - coordinates
properties:
  coordinates:
    type: array
    minItems: 2
    description: Az útvonal vonalgeometriája, [lon, lat] pontsorozatként.
    items:
      type: array
      minItems: 2
      maxItems: 2
      items:
        type: number
