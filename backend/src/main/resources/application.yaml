spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB:lm2}
    username: ${POSTGRES_USER:lm2}
    password: ${POSTGRES_PASSWORD:change-me}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

lm2:
  jwt:
    secret: ${LM2_JWT_SECRET}
  admin:
    api-key: ${LM2_ADMIN_API_KEY}
